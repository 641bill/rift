# Checked Overhead Removal Matrix

Date: 2026-05-03
Last updated: 2026-06-15 11:30 CEST

Status: overhead-removal contract plus implementation checkpoint. This matrix
separates three states:

- already removed or avoided in code;
- safe to remove only after explicit compiler/runtime probes;
- still required because it is real operator/container work, not memory-safety
  bookkeeping.

## Current-Head Focused Validations

| Item | Current evidence | Status |
|---|---|---|
| `RiftRegion.ownedEitherFold` branch-specific abstract `Either` extraction residual heap allocation | The 2026-06-15 compiler/runtime proof adds a source-level object-only fold bridge for `Either[A, B]^{region}` when branch payloads are already owned by the same checked region. The helper preserves full branch payload types, so callback parameter owners are inferred from the region-owned `Either` instead of explicitly ascribed at each call site. Runtime stats prove the branch case objects, wrapper records, wrapper closure values, and closure-body payloads allocate in checked-region memory (`delta >= 8`). Compiler negatives prove unrooted heap metadata hidden behind a folded branch payload remains rejected. Follow-up compiler boundary `inferredOwnedEitherFoldStillNeedsTypedBranchHelpers` proves untyped `ownedLeft`/`ownedRight` construction still widens owner flow under expected abstract `Either`; compiler validation passed `784/784` at 2026-06-15 11:30 CEST, with runtime `349/349` and `sandbox3_next` compile unchanged from 11:16. | Promoted as focused residual-heap-allocation removal for source-level branch-specific abstract `Either` folding through the helper. This is not raw abstract `Either` pattern-match inference, expected-owner propagation for untyped branch helpers, general callback owner recovery outside this helper, branch/match selection, primitive box placement, boxed-key placement, or unknown library inference. No elapsed/profile improvement is claimed yet. |
| Raw concrete `Left(...)` / `Right(...)` direct-nested wrapper-payload residual heap allocation | The 2026-06-15 compiler/runtime proof extends the existing concrete branch constructor rewrite evidence to direct-nested wrapper payloads. Runtime stats prove concrete `Left`/`Right` case objects, wrapper records, and nested payloads allocate in checked-region memory (`delta >= 6`). The follow-up compiler negative proves raw `Left(new Wrapper(new Entry(metadata)))` still rejects unrooted heap metadata hidden inside the nested payload. Validation passed compiler `781/781`, runtime `348/348` after a clean native rebuild, and `sandbox3_next` compile at 2026-06-15 10:22 CEST. | Promoted as focused residual-heap-allocation removal evidence for concrete branch construction assigned to owner-typed immutable locals. This is not abstract `Either[A, B]` construction, raw branch/match selection, primitive box placement, boxed-key placement, or unknown library inference. No elapsed/profile improvement is claimed yet. |
| Raw concrete `Left.value` / `Right.value` direct-nested wrapper-payload residual heap allocation | The 2026-06-14 compiler/runtime proof extends the existing concrete branch accessor rewrite evidence to direct-nested wrapper payloads. Runtime stats prove concrete `Left`/`Right` case objects, wrapper records, and nested payloads allocate in checked-region memory (`delta >= 6`) while raw `.value` extraction preserves owner flow. Compiler negatives prove unrooted heap metadata hidden in the extracted nested payload remains rejected. Validation passed compiler `779/779`, runtime `347/347` after a clean native rebuild, and `sandbox3_next` compile at 2026-06-14 18:53 CEST. | Promoted as focused residual-heap-allocation removal evidence for concrete branch `.value` extraction assigned to owner-typed immutable locals. This is not abstract `Either[A, B]` pattern extraction, raw branch/match selection, primitive box placement, boxed-key placement, or unknown library inference. No elapsed/profile improvement is claimed yet. |
| Raw `Some(...)` / `Option(...)` direct-nested wrapper-payload residual heap allocation | The 2026-06-14 compiler/runtime proof extends the narrowed raw option rewrite to immutable owner-typed locals whose constructor argument is a direct region construct containing another direct region construct, such as `Some(new Wrapper(new Box(...)))` and `Option(new Wrapper(new Box(...)))`. Runtime stats prove raw `Some`/`Option` wrappers, nested wrapper records, and nested payloads allocate in checked-region memory (`delta >= 6`). Compiler negatives prove unrooted heap metadata hidden in the nested payload remains rejected. Validation passed compiler `777/777`, runtime `346/346` after a clean native rebuild, and `sandbox3_next` compile at 2026-06-14 16:07 CEST. | Promoted as focused residual-heap-allocation removal for direct-nested wrapper-payload raw `Some`/`Option` construction assigned to owner-typed immutable locals. This deliberately does not claim selected/match method-returned wrapper-payload placement, arbitrary raw option inference, primitive boxes, boxed-key paths, generic library containers, or unknown library paths. No elapsed/profile improvement is claimed yet. |
| Raw `Some(...)` / `Option(...)` direct-nested wrapper-closure residual heap allocation | The 2026-06-13 compiler/runtime proof adds a narrowed pre-capture rewrite for immutable owner-typed locals whose RHS is raw `Some(...)` or `Option(...)` and whose constructor argument contains nested closure allocation effects. The rewrite delegates to owner-token-specific non-inline compiler bridges only when the expected owner-typed result proves one checked owner and a concrete scoped/open/open-handle runtime owner value is available. Runtime stats prove raw `Some`/`Option` wrappers, wrapper records, closure values, and closure-body payloads allocate in checked-region memory (`delta >= 8`). Compiler negatives prove durable-heap escape and unrooted heap metadata remain rejected. Validation passed compiler `775/775`, runtime `345/345` after a forced clean native rebuild, and `sandbox3_next` compile at 2026-06-13 11:29 CEST. | Promoted as focused residual-heap-allocation removal for direct-nested wrapper-closure raw `Some`/`Option` construction assigned to owner-typed immutable locals. A broader raw-option rewrite was rejected during validation because it displaced existing direct `Some(new Payload)` allocation marking; ordinary direct payload shapes stay on their previously validated path. This does not remove raw branch/match selection, ambiguous owner constructors, primitive boxes, boxed-key paths, generic library containers, or unknown library paths, and no elapsed/profile improvement is claimed yet. |
| Concrete raw `Left(...)` / `Right(...)` wrapper-closure construction residual heap allocation | The 2026-06-12 compiler/runtime proof adds a pre-capture rewrite for immutable owner-typed locals whose RHS is a concrete raw `Left(...)` or `Right(...)` constructor. The rewrite delegates to non-inline compiler bridges only when the expected type is a concrete `Left[A^{region}, B^{region}]^{region}` or `Right[A^{region}, B^{region}]^{region}`, the expected type has one checked owner, and a concrete scoped/open/open-handle runtime owner value is available. Runtime stats prove two concrete case objects, two wrapper records, two closure values, and two closure-body payloads allocate in checked-region memory (`delta >= 8`). Compiler negatives prove durable-heap escape and unrooted heap metadata remain rejected. Validation passed compiler `771/771`, runtime `344/344`, and `sandbox3_next` compile at 2026-06-12 10:23 CEST. | Promoted as focused residual-heap-allocation removal for concrete raw branch construction assigned to owner-typed immutable locals. This does not remove abstract `Either[A, B]` construction, branch/match selection, abstract pattern extraction, ambiguous owner constructors, primitive boxes, boxed-key paths, or unknown library paths, and no elapsed/profile improvement is claimed yet. |
| Concrete raw `Left.value` / `Right.value` wrapper-closure extraction residual heap allocation | The 2026-06-11 compiler/runtime proof adds a pre-capture rewrite for immutable owner-typed locals whose RHS is a concrete raw `Left.value` or `Right.value` accessor. The rewrite delegates to the existing owner-preserving branch helper only when the qualifier is a concrete `Left[A^{region}, B^{region}]^{region}` or `Right[A^{region}, B^{region}]^{region}`, the expected result has one checked owner, and the qualifier has the same concrete runtime owner. Runtime stats prove two concrete case objects, two wrapper records, two closure values, and two closure-body payloads allocate in checked-region memory (`delta >= 8`). Compiler negatives prove durable-heap escape and unrooted heap metadata remain rejected. Validation passed compiler `768/768`, runtime `343/343`, and `sandbox3_next` compile at 2026-06-11 15:08 CEST. | Promoted as focused residual-heap-allocation removal for concrete branch `.value` access assigned to owner-typed immutable locals. This does not remove abstract `Either[A, B]` pattern matching, branch/match selection, ambiguous owner accessors, primitive boxes, boxed-key paths, or unknown library paths, and no elapsed/profile improvement is claimed yet. |
| `RiftRegion.ownedSelect` selected same-owner `Tuple2` wrapper-closure residual heap allocation | The 2026-06-11 compiler/runtime proof extends non-allocating object-only `RiftRegion.ownedSelect` to helper-backed `Tuple2[Wrapper[T^{r}]^{r}, Wrapper[T^{r}]^{r}]^{r}` values built with `ownedTuple2`, then extracted with `_1` / `_2`. Runtime stats prove two tuple objects, four wrapper records, four closure values, and four closure-body payloads allocate in checked-region memory (`delta >= 14`). Compiler negatives prove the selected helper path rejects durable-heap escape and unrooted heap metadata. Validation passed compiler `765/765`, runtime `342/342`, and `sandbox3_next` compile at 2026-06-11 13:51 CEST. | Promoted as focused residual-heap-allocation removal for helper-backed same-owner `Tuple2` selection. This does not remove raw tuple syntax, raw branch/match selection, primitive boxes, boxed-key paths, or unknown library paths, and no elapsed/profile improvement is claimed yet. |
| Hidden/type-only closure-body effect placement | The 2026-06-11 validation found that adding hidden effect-owner method parameters without rewriting the closure class environment and every generated adapter can produce NIR arity mismatches. The backend now disables hidden effect-owner method parameters and keeps type-only hidden-owner closure bodies on heap fallback unless a concrete runtime owner is available through normal captures or another proven owner term. Runtime tests pin the fallback at one region object for the closure value only; compiler tests show unrooted heap metadata is accepted in these fallback bodies because no checked-region allocation stores it. | Not removed. This is a safety correction, not a performance optimization. Full hidden-owner placement still requires lambda environment/signature rewriting plus adapter coverage before runtime checks or heap fallback can be relaxed. |
| `RiftRegion.ownedSelect` selected same-owner `Either` wrapper-closure residual heap allocation | The 2026-06-11 compiler/runtime proof extends non-allocating object-only `RiftRegion.ownedSelect` to helper-backed `Either[Wrapper[T^{r}]^{r}, Wrapper[T^{r}]^{r}]^{r}` values built with `ownedLeft` / `ownedRight`, then extracted with `ownedEitherValue`. Runtime stats prove four `Either` case objects, four wrapper records, four closure values, and two closure-body payloads allocate in checked-region memory (`delta >= 14`). Compiler negatives prove the selected helper path rejects durable-heap escape and unrooted heap metadata. Validation passed compiler `762/762`, runtime `341/341`, and `sandbox3_next` compile at 2026-06-11 11:15 CEST. | Promoted as focused residual-heap-allocation removal for helper-backed same-owner `Either` selection. This does not remove raw branch/match selection, unannotated selected `Either` locals before extraction, abstract `Either[A, B]` pattern matching, primitive boxes, boxed-key paths, or unknown library paths, and no elapsed/profile improvement is claimed yet. |
| `RiftRegion.ownedSelect` selected same-owner wrapper-closure residual heap allocation | The 2026-06-11 compiler/runtime proof adds non-allocating object-only `RiftRegion.ownedSelect[A <: Object^](using region)(chooseFirst, first, second)` for values already owned by the same checked region. Runtime stats prove four helper-backed `Some`/`Option` wrappers, four wrapper records, four closure values, and two closure-body payloads allocate in checked-region memory (`delta >= 14`) before/through the selected wrapper. Compiler negatives prove the selected helper path rejects durable-heap escape and unrooted heap metadata. Validation passed compiler `759/759`, runtime `340/340`, and `sandbox3_next` compile at 2026-06-11 10:07 CEST. | Promoted as focused residual-heap-allocation removal for same-owner helper selection. This does not remove raw `if` / `match` selection of `Option[Wrapper[T^{r}]^{r}]^{r}` values, primitive boxes, boxed-key paths, or unknown library paths, and no elapsed/profile improvement is claimed yet. |
| `RiftRegion.ownedTuple2` direct-nested wrapper-closure residual heap allocation | The 2026-06-10 compiler/runtime proof promotes direct-nested wrapper-closure operands through the existing object-only `RiftRegion.ownedTuple2` helper bridge. Runtime stats prove three tuple objects, six direct-nested wrapper records, six closure values, and six closure-body payloads allocate in checked-region memory (`delta >= 21`). Compiler negatives prove the helper rejects durable-heap escape and unrooted heap metadata. Validation passed compiler `756/756`, runtime `339/339`, and `sandbox3_next` compile at 2026-06-10 15:38 CEST. | Promoted as focused residual-heap-allocation removal for direct-nested wrapper-closure values through a reusable capture-preserving tuple helper. This does not remove raw `Tuple2(new Wrapper(...), new Wrapper(...))`, tuple literal syntax, primitive boxes, boxed-key paths, or unknown library paths, and no elapsed/profile improvement is claimed yet. |
| `RiftRegion.ownedLeft` / `ownedRight` direct-nested wrapper-closure residual heap allocation | The 2026-06-10 compiler/runtime proof promotes direct-nested wrapper-closure operands through the existing object-only `RiftRegion.ownedLeft` / `ownedRight` helper bridges. Runtime stats prove two concrete `Either` case objects, two direct-nested wrapper records, two closure values, and two closure-body payloads allocate in checked-region memory (`delta >= 8`). Compiler negatives prove the helpers reject durable-heap escape and unrooted heap metadata. Validation passed compiler `753/753`, runtime `338/338`, and `sandbox3_next` compile at 2026-06-10 15:21 CEST. | Promoted as focused residual-heap-allocation removal for direct-nested wrapper-closure values through reusable capture-preserving `Either` helpers. At this checkpoint raw `Left(new Wrapper(closure))` / `Right(new Wrapper(closure))` syntax and raw `.value` remained fallback; later current-head rewrites cover concrete owner-typed immutable local raw construction and extraction. Abstract `Either[A, B]` pattern matching, primitive boxes, boxed-key paths, and unknown library paths remain unresolved, and no elapsed/profile improvement is claimed yet. |
| `RiftRegion.ownedSome` / `ownedOption` direct-nested wrapper-closure residual heap allocation | The 2026-06-10 compiler/runtime proof promotes object-only helper bridges `RiftRegion.ownedSome[A <: Object^](using region)(value)` and `RiftRegion.ownedOption[A <: Object^](using region)(value)` for values already owned by the same checked region. Runtime stats prove three non-null `Some`/`Option` wrappers, three direct-nested wrapper records, three closure values, and three closure-body payloads allocate in checked-region memory (`delta >= 12`). Compiler negatives prove the helpers reject durable-heap escape and unrooted heap metadata. Validation passed compiler `750/750`, runtime `337/337`, and `sandbox3_next` compile at 2026-06-10 14:11 CEST. | Promoted as focused residual-heap-allocation removal for direct-nested wrapper-closure values through reusable capture-preserving `Some`/`Option` helpers. The later 2026-06-13 rewrite covers the corresponding owner-typed immutable local raw `Some(new Wrapper(closure))` / `Option(new Wrapper(closure))` syntax. Primitive boxes, boxed-key paths, raw branch/match selection, ambiguous owners, and unknown library paths remain unresolved, and no elapsed/profile improvement is claimed yet. |
| `RiftRegion.ownedLeftValue` / `ownedRightValue` branch-specific extraction residual heap allocation | The 2026-06-10 compiler/runtime proof promotes concrete branch extraction bridges `RiftRegion.ownedLeftValue[A <: Object^, B <: Object^](using region)(left)` and `RiftRegion.ownedRightValue[A <: Object^, B <: Object^](using region)(right)` for `Left[A^{region}, B^{region}]^{region}` and `Right[A^{region}, B^{region}]^{region}`. `ownedLeft` / `ownedRight` now return concrete case types, so branch-preserving callers can extract unrelated branch payload wrappers. Runtime stats prove two concrete case objects, two wrapper records, two closure values, and two closure-body payloads allocate in checked-region memory (`delta >= 8`). Compiler negatives prove the helpers reject durable-heap escape and unrooted heap metadata. Validation passed compiler `747/747`, runtime `336/336`, and `sandbox3_next` compile at 2026-06-10 13:08 CEST. | Promoted as focused residual-heap-allocation removal for reusable concrete-branch `Either` extraction. At this checkpoint raw `.value` syntax still widened ownership; the later 2026-06-11 current-head rewrite covers only concrete owner-typed immutable local raw accessors. Pattern matching over abstract `Either[A, B]`, primitive boxes, boxed-key paths, and unknown library paths remain unresolved, and no elapsed/profile improvement is claimed yet. |
| `RiftRegion.ownedEitherValue` wrapper-closure extraction residual heap allocation | The 2026-06-09 compiler/runtime proof promotes a same-type extraction bridge `RiftRegion.ownedEitherValue[A <: Object^](using region)(either)` for `Either[A^{region}, A^{region}]^{region}`. Runtime stats prove three `Either` case objects, three wrapper records, three closure values, and three closure-body payloads allocate in checked-region memory (`delta >= 12`) after owner-preserving extraction. Compiler negatives prove the helper rejects durable-heap escape and unrooted heap metadata. Validation passed compiler `744/744`, runtime `335/335`, and `sandbox3_next` compile at 2026-06-09 16:25 CEST. | Promoted as focused residual-heap-allocation removal for reusable same-owner `Either` extraction. The helper is deliberately non-inline and uses a cast boundary after the type proves same-owner, same-type ownership because raw inline `Left.value` / `Right.value` widens ownership in this checkpoint. The later 2026-06-11 current-head rewrite covers only concrete owner-typed immutable local raw accessors; abstract `Either[A, B]` pattern-match extraction, primitive boxes, boxed-key paths, and unknown library paths remain unresolved, and no elapsed/profile improvement is claimed yet. |
| `RiftRegion.ownedLeft` / `ownedRight` wrapper-closure residual heap allocation | The 2026-06-09 compiler/runtime proof promotes typed `Left`/`Right` construction into object-only helpers `RiftRegion.ownedLeft[A <: Object^, B <: Object^](using region)(value)` and `RiftRegion.ownedRight[A <: Object^, B <: Object^](using region)(value)`. Runtime stats prove three `Either` case objects, three wrapper records, and three inline closure values allocate in checked-region memory (`delta >= 9`) for owner-typed wrapper-closure values. Compiler negatives prove the helpers reject durable-heap escape and unrooted heap metadata. Validation passed compiler `741/741`, runtime `334/334`, and `sandbox3_next` compile at 2026-06-09 16:04 CEST. | Promoted as focused residual-heap-allocation removal for reusable capture-preserving `Either` construction helpers, with safety boundaries covered for escape and unrooted metadata. The later direct-nested helper proof covers `ownedLeft/ownedRight(new Wrapper(closure))`; later `ownedEitherValue` and `ownedLeftValue` / `ownedRightValue` bridges cover same-owner same-type or concrete-branch extraction; the 2026-06-11 rewrite covers concrete owner-typed immutable local raw `Left.value` / `Right.value` accessors; and the 2026-06-12 rewrite covers concrete owner-typed immutable local raw `Left(...)` / `Right(...)` construction. Abstract/pattern/branch-selected extraction, primitive boxes, boxed-key paths, and unknown library paths remain unresolved, and no elapsed/profile improvement is claimed yet. |
| `RiftRegion.ownedTuple2` wrapper-closure residual heap allocation | The 2026-06-07 compiler/runtime proof promotes the validated local capture-preserving factory into the object-only helper `RiftRegion.ownedTuple2[A <: Object^, B <: Object^](using region)(left, right)`. Runtime stats prove three tuple objects, six wrapper records, and six inline closure values allocate in checked-region memory (`delta >= 15`) for owner-typed wrapper-closure locals. Compiler negatives prove the helper rejects durable-heap escape and unrooted heap metadata retained by closure-body region allocation. Validation passed compiler `738/738`, runtime `333/333`, and `sandbox3_next` compile at 2026-06-07 22:02 CEST; the later 2026-06-10 direct-nested helper proof covers `ownedTuple2(new Wrapper(closure), new Wrapper(closure))` with closure-body allocation. | Promoted as focused residual-heap-allocation removal for the reusable capture-preserving tuple helper, with safety boundaries covered for escape and unrooted metadata. This validates the library/API bridge direction but does not remove raw untyped `Tuple2(first, second)`, raw direct-nested `Tuple2(new Wrapper(...), new Wrapper(...))`, tuple literal fallbacks, primitive boxes, or boxed-key paths, and no elapsed/profile improvement is claimed yet. |
| Untyped tuple wrapper-closure frontend boundary | The 2026-06-07 phase audit shows these failures happen before Rift placement lowering: `PrepNativeInterop` starts after `PostTyper`, `RiftRegionInference` runs after post-inline interop, and `GenNIR` runs after `MoveStatics`. Rejected untyped tuple sources never reach a point where a Scala Native plugin-only allocation-zone bridge can safely recover owner flow. | Not removable in the Scala Native lowering layer. Needs capture-preserving tuple factory/library signatures or a Scala capture-checking expected-owner propagation bridge. Heap fallback remains sound. |
| Tuple-literal owner-typed-local-operands wrapper-closure residual heap allocation | The compiler negative `inferredForwardedMethodReturnedTupleLiteralWrapperLocalOperandsCaptureWideningFallsBack` proves tuple literal `(first, second)` still widens wrapper element ownership even when `first` and `second` are already explicit `Wrapper[T^{r}]^{r}` locals. Validation passed compiler `732/732` and `sandbox3_next` compile on 2026-06-07 19:00 CEST. | Not removed. Tuple literal syntax follows the same untyped tuple construction fallback as `Tuple2(first, second)`. Explicit `Tuple2[Wrapper[T^{r}]^{r}, Wrapper[T^{r}]^{r}](...)` remains the promoted safe spelling. No runtime allocation-stat or elapsed/profile improvement is claimed. |
| Untyped `Tuple2` owner-typed-local-operands wrapper-closure residual heap allocation | The compiler negative `inferredForwardedMethodReturnedUntypedTupleWrapperLocalOperandsCaptureWideningFallsBack` proves `Tuple2(first, second)` still widens wrapper element ownership even when `first` and `second` are already explicit `Wrapper[T^{r}]^{r}` locals containing checked-owner closures. Validation passed compiler `731/731` on 2026-06-05 10:45 CEST; sandbox/runtime were not rerun because follow-up escalation was rejected after the usage limit was hit. | Not removed. This pins the fallback to untyped `Tuple2.apply` expected-type/capture preservation. Explicit `Tuple2[Wrapper[T^{r}]^{r}, Wrapper[T^{r}]^{r}](...)` remains the promoted safe spelling. No runtime allocation-stat or elapsed/profile improvement is claimed. |
| Local-ascribed untyped direct-nested `Tuple2` wrapper-closure residual heap allocation | The compiler negative `inferredForwardedMethodReturnedLocalAscribedUntypedTupleWrapperCaptureWideningFallsBack` proves a local explicitly typed as `Tuple2[Wrapper[T^{r}]^{r}, Wrapper[T^{r}]^{r}]^{r}` still cannot safely receive untyped `Tuple2(new Wrapper(...), new Wrapper(...))`: capability `r` cannot flow into the fresh tuple capture set. Validation passed compiler `730/730` and `sandbox3_next` compile on 2026-06-05 10:36 CEST. | Not removed. Source-local owner ascription is not a sufficient workaround; this remains heap fallback until expected-type/source-summary preservation or a capture-preserving tuple factory path is implemented. No runtime allocation-stat or elapsed/profile improvement is claimed. |
| Untyped direct-nested `Tuple2` wrapper-closure residual heap allocation | The compiler negative `inferredForwardedMethodReturnedUntypedDirectNestedTupleWrapperCaptureWideningFallsBack` proves `Tuple2(new Wrapper(...), new Wrapper(...))` still widens the nested wrapper owner even when the method result is explicitly `Tuple2[Wrapper[T^{r}]^{r}, Wrapper[T^{r}]^{r}]^{r}`. Validation passed compiler `729/729` and `sandbox3_next` compile on 2026-06-05 10:18 CEST. | Not removed. This remains a source-capture/expected-type preservation gap, so heap fallback is sound. No runtime allocation-stat or elapsed/profile improvement is claimed. |
| Direct-nested typed `Tuple2` wrapper-closure residual heap allocation | Explicitly typed `Tuple2[Wrapper[T^{r}]^{r}, Wrapper[T^{r}]^{r}](new Wrapper(...), new Wrapper(...))` now has allocation-stat proof when both direct nested wrappers contain checked-owner closures, including direct and branch-forwarding method wrappers. Runtime stats prove three tuple objects, six wrapper records, and six closure values allocate in checked-region memory (`delta >= 15`). Validation passed compiler `728/728`, runtime `331/331`, and `sandbox3_next` compile on 2026-06-05. | Promoted as focused residual-heap-allocation removal for direct-nested typed tuple wrapper-closure value chains. No elapsed/profile claim yet; the compiler negatives above record that raw-untyped, local-ascribed untyped, owner-typed-local-operand untyped, and tuple-literal tuple construction remain capture-widening fallbacks, and tuple wrapper-payload extraction also remains fallback. |
| Typed `Tuple2` field-extraction closure-body residual heap allocation | Explicitly typed `Tuple2[Wrapper[T^{r}]^{r}, Wrapper[T^{r}]^{r}](first, second)` now has allocation-stat proof through `_1`/`_2` when the returned tuple and extracted wrappers are bound as owner-typed locals before invoking their closures. Runtime stats prove three tuple objects, six wrapper records, six closure values, and six closure-body payloads allocate in checked-region memory (`delta >= 21`). Validation passed compiler `726/726`, runtime `330/330`, and `sandbox3_next` compile on 2026-06-05. | Promoted as focused residual-heap-allocation removal for the source-expressible typed tuple field-extraction path. No elapsed/profile claim yet; untyped direct nested tuple construction, untyped tuple extraction, and tuple wrapper-payload extraction remain capture-widening fallbacks. |
| Typed `Tuple2` wrapper-closure residual heap allocation | Explicitly typed `Tuple2[Wrapper[T^{r}]^{r}, Wrapper[T^{r}]^{r}](first, second)` now has allocation-stat proof when `first` and `second` are owner-typed generic wrappers containing checked-owner closures, including simple direct and branch-forwarding method wrappers. Runtime stats prove three tuple objects, six wrapper records, and six closure values allocate in checked-region memory (`delta >= 15`). Validation passed compiler `725/725`, runtime `329/329`, and `sandbox3_next` compile on 2026-06-04. | Promoted as focused residual-heap-allocation removal for returned typed tuple wrapper-closure value chains. Newer rows cover explicitly owner-typed `_1`/`_2` closure execution and direct-nested typed tuple construction; untyped extraction remains a capture-widening fallback. |
| Typed `Either` wrapper-closure residual heap allocation | Explicitly typed `Left[Wrapper[T^{r}]^{r}, Wrapper[T^{r}]^{r}](wrapper)` and `Right[Wrapper[T^{r}]^{r}, Wrapper[T^{r}]^{r}](wrapper)` now have allocation-stat proof when `wrapper` is an owner-typed generic wrapper containing a checked-owner closure, including simple direct and branch-forwarding method wrappers. Runtime stats prove three `Either` case wrappers, wrapper records, and closure values allocate in checked-region memory (`delta >= 9`). Validation passed compiler `724/724`, runtime `328/328`, and `sandbox3_next` compile on 2026-06-04. | Promoted as focused residual-heap-allocation removal for returned `Either` wrapper-closure value chains. No elapsed/profile claim yet; nested closure-body execution through `Left.value`/`Right.value` remains blocked by wrapper field-extraction capture widening. |
| Typed `Option.apply` wrapper-closure residual heap allocation | The typed wrapper-closure method-summary shape now also has allocation-stat proof through null-preserving `Option[Wrapper[T^{r}]^{r}](wrapper)`, including simple direct and branch-forwarding method wrappers. Runtime stats prove three non-null `Option.apply` branches allocate their `Some` objects, wrapper records, closure values, and closure-body payloads in checked-region memory (`delta >= 12`). Validation passed compiler `723/723`, runtime `327/327`, and `sandbox3_next` compile on 2026-06-04. | Promoted as the null-preserving counterpart to the typed `Some` wrapper-closure residual-allocation slice. No elapsed/profile claim yet; the later `RiftRegion.ownedOption` helper covers the direct-nested wrapper-closure helper path, while raw `Option(new Wrapper(closure))` remains a frontend capture-widening fallback. |
| Typed `Some` wrapper-closure residual heap allocation | Explicit checked-region methods now have allocation-stat proof for the source-expressible staged shape `val wrapper: Wrapper[T^{r}]^{r} = new Wrapper(closure); Some[Wrapper[T^{r}]^{r}](wrapper)`, including simple direct and branch-forwarding method wrappers. Runtime stats prove the `Some` object, wrapper record, closure value, and closure-body payload are checked-region allocations (`delta >= 4` for one chain and `delta >= 12` for three forwarded chains). At this checkpoint, the direct nested spelling `Some(new Wrapper(closure))` remained a compiler negative because Scala capture checking widened the generic closure field before Rift could recover the owner. Validation passed compiler `722/722`, runtime `326/326`, and `sandbox3_next` compile on 2026-06-04. | Promoted as focused residual-heap-allocation removal for direct and forwarded method-returned synthetic wrapper/closure shapes. No elapsed/profile claim yet; the later `RiftRegion.ownedSome` helper covers the direct-nested wrapper-closure helper path, and the 2026-06-13 raw `Some`/`Option` rewrite covers the narrowed owner-typed immutable local raw wrapper-closure construction path. |
| Hidden-owner closure-body `Option` wrapper residual heap allocation | Historical 2026-06-04 proof was superseded on 2026-06-11 after the backend stopped adding hidden effect-owner parameters to lambda-lifted/adapted closure-body methods without coordinated closure-environment and adapter rewriting. Current runtime tests `scopedRegionEffectPolymorphicClosureWithHiddenOwnerFallsBack` and `scopedRegionEffectPolymorphicClosureOptionFactoryWithHiddenOwnerFallsBack` pin heap fallback for the body/wrapper allocation path (`delta == 1`, closure value only). Compiler fallback tests `effectPolymorphicClosureBodyHiddenOwnerFallsBackWithHeapMetadata` and `effectPolymorphicClosureBodyOptionFactoryHiddenOwnerFallsBackWithHeapMetadata` compile because no checked-region allocation stores unrooted heap metadata. Validation passed compiler `765/765`, runtime `342/342`, and `sandbox3_next` compile on 2026-06-11. | Not promoted as current overhead removal. Full hidden/type-only owner placement remains future work until lambda signature/environment rewriting supplies a concrete checked runtime owner value to every lowered method and adapter. |
| Closure-body returned closure residual heap allocation | Historical 2026-06-03 proof is now scoped to captured-owner shapes whose concrete runtime owner is available through normal capture or another proven lowered owner term. The old hidden effect-owner bridge is no longer promoted because it can desynchronize lambda-lifted closure-body signatures from generated closure adapters. Current validation keeps heap fallback for type-only hidden-owner body effects while preserving region placement for captured-owner helper paths. | Partially promoted only for captured-owner closure/effect summaries. Hidden-owner returned-closure placement is not a current overhead-removal claim; it needs the same lambda environment/signature rewrite as the hidden-owner `Option` body path. |
| `putFoldInBucket` redundant open check removal | Child `685f2b67e` removes an explicit `bucket.child.checkOpen()` and delegates the open check to `appendWindowUnchecked`. Standard validation is restored after gating broad automatic local-escape scopes off by default (`718/718` compiler, `322/322` runtime, sandbox compile passed). Focused 1M x3 rerun from the validated worktree reports heap `99.302 ms`, checked Rift `97.321 ms`, same checksum `-6322631816023653086`, checked GC `0.000 ms`, Rift op `0.148 ms`, and RSS `40.4 MB` checked versus `75.0 MB` heap. | Promoted as focused overhead-removal evidence for `StreamWindowFold`. Still requires application-specific smokes/L1/L2 before using Common Crawl WET, NEXMark Q5, or DEBS fold rows as application claims. |

## Static Safety To Runtime Work Mapping

| Static property | Runtime work that should be absent from checked headline paths | Current state |
|---|---|---|
| Region values cannot escape their lifetime. | No runtime escape table, no Yak-style promotion barrier. | Satisfied by compiler probes for current checked API slice. |
| Heap objects cannot retain region values past close. | No dynamic heap-to-region retention tracker. | Satisfied by negative compiler probes for direct retention patterns. |
| Region-to-heap references require immutable/static metadata or explicit `HeapRoot`. | No blanket per-page root registration for checked Rift regions. | Partially satisfied; root-free SafeZone-backed lowering is not a safety claim yet. |
| Outer-to-inner region references are rejected unless lifetimes are equal/proven. | No close-time scan to discover illegal outer references. | Partially satisfied by owner-token APIs and negative probes. |
| Escaping closures cannot capture region values. | No runtime closure-capture tracker. | Satisfied by current negative compiler probes. |
| Use-after-close/reset is rejected by API/lifetime shape. | Hot paths should not rely on dynamic use-after-close checks for correctness. | Public low-level APIs remain defensive. `OpenStreamingRegion` is now compiler-guarded against direct user `close()`/`reset()`, and operator-owned page/token paths remove selected checks after stale-bucket probes. |
| Diagnostics are opt-in. | No per-allocation global atomics for byte counters or trace stats. | Already improved for Rift allocation stats; SafeZone trace remains diagnostic-only. |

## Already Removed Or Avoided

| Overhead | Change / evidence | Result |
|---|---|---|
| Rift per-allocation global allocated-byte atomics | Default fast path now accumulates total raw bytes locally and flushes at close/reset. | Common Crawl-like q1/q2 ordering changed in favor of trusted Rift. This is the clearest completed overhead-removal item. |
| SafeZone per-page root-removal cliff | Improved roots mode coalesces root removal. | `safezone-improved` is now the real SafeZone baseline. |
| Per-entry close callback in append-window API | `StreamAppendCursor` closes buckets with one bucket callback and cursor traversal. | Focused 1M append-window cursor gate passed. |
| Per-record bucket open check and child-region lookup in the page/token append path | `StreamPageTokenAppendWindow` owns bucket lookup, caches the child region once per bucket, and appends through an owned path that skips per-record `bucket.child.checkOpen()` and stale-current `isOpen` checks. | Focused 1M `rift-checked-page-token` is `27.141 ms` vs current `rift-checked-rift` `30.819 ms`; generated Common Crawl-shaped q1/q2 checked rows improve by `16.2-18.5%` versus current checked. |
| Page-token no-drain close for append-only/aggregate-on-append shapes | `closePageTokenAppendBucketsBeforeNoDrain` and `closeAllPageTokenAppendBucketsNoDrain` clear parent bucket refs and close child regions without cursor traversal when the operator has already completed query work on append. | Safety/runtime probes pass, but the 1M cost matrix did not materially improve. Keep as an operator-owned option, not a headline speedup. |
| Generic checked `allocImpl/checkOpen` in operator-owned page-token child buckets | `OpenStreamingRegion` plus `allocOpen(...)` lets lowering call `RiftRegion.allocUncheckedImpl(...)` only when the page-token operator owns bucket open/close order. | Validation passes. Focused 1M checked scoped page-token is `73.632/83.177/82.198 ms` on append-only/drain/aggregate versus heap `76.295/85.873/84.354 ms`; DSPBench Fraud q2 improves slightly to `797.782 ms` checked scoped versus heap `806.697 ms`. |
| Direct user close/reset on open allocation handles | `OpenStreamingRegion` is the internal allocation-capable handle; lifecycle must be owned by `epoch`, page-token, or window operators. | Compiler probes now reject `epoch.close()` and `epoch.reset()` through an open handle, while the Rift runtime implementation remains allowed to close the child in its owning boundary. This closes the first active/closed typestate gap behind the existing `allocOpen` fast path. |
| Missing mixed-reference probes on operator-owned open paths | Static/immutable metadata and explicit `HeapRoot` bridge handles need to work through the same open allocation paths used by page/window and epoch-buffer operators. | Compiler probes now cover static metadata, `HeapRoot`, and unrooted dynamic metadata rejection for page-token append, page-token map/filter, page-token count-by-key, and epoch-buffer `allocOpen` paths. |
| Public stale page-token child allocation after close | Public defensive region handles must still reject use-after-close even when operator-owned fast paths skip checks. | Runtime probes now reject allocation through a public page-token child region after `closeAllPageTokenAppendBucketsWithCursor` for both Rift and SafeZone-backed checked regions. |
| Rift allocation counters in L1 final-clean runs | `RIFT_FINAL_CLEAN=1` now disables Rift raw/object/byte allocation counters by default; `RIFT_ALLOC_STATS=1` forces them back on for diagnostics. | Focused 5M `ObjectAllocationLoweringMatrix` `rift-checked-rift` improves from `87.999 ms` with counters forced on to `76.345 ms` with counters disabled, same checksum and zero GC. Application gates are workload-sensitive: StreamFlex-design checked stream improves from `9.18 s` to `8.57 s`, Common Crawl-shaped checked page-token improves from `4.33 s` to `4.23 s`, while Dataflow/Yak process-level gates are noisy. L2/stat rows keep counters enabled by default. |
| Generic raw-allocation path in Rift managed-object allocation | Managed object allocation always needs pointer alignment, so the checked Rift object path no longer needs the generic raw-allocation alignment normalizer in the hot path. | Focused 5M `ObjectAllocationLoweringMatrix` `rift-checked-rift` improves from the previous final-clean `76.345 ms` to `69.912 ms`, same checksum and zero GC. Single-run application gates also move: StreamFlex-design checked stream `8.57 s -> 8.07 s`, Common Crawl-shaped checked page-token `4.23 s -> 3.98 s`. Object zero/init and header setup are unchanged. |
| Extra superclass hop in final open-region `allocUncheckedImpl` | Final `OpenStreamingRegion` implementations call the Rift/SafeZone allocator directly for operator-owned `allocOpen`, while public defensive `allocImpl` stays unchanged. | Validation passes. Clean focused final-clean 1M page-token rows are heap `38.378 ms`, checked Rift `25.007 ms`, and checked SafeZone-backed `25.591 ms`. Clean StreamFlex-design 20M x3 L1 rerun is heap `31.26 s`, checked scoped `24.39 s`, checked stream `22.81 s`; generated Common Crawl-shaped q1/q2 checked Rift page-token is `10.88/11.49 s` versus heap `17.49/16.73 s`. |
| Per-allocation allocation-stats enabled lookup in final-clean Rift regions | `scalanative_rift_region` now caches allocation-stats mode at open, so raw/object/slow allocation paths read the region flag instead of calling `scalanative_rift_alloc_stats_enabled()` per allocation. `RIFT_ALLOC_STATS=1` still forces diagnostic counters. | The post-change L4 profiles for StreamFlex-design checked stream and Common Crawl-shaped q2 no longer sample `scalanative_rift_alloc_stats_enabled`. Focused 5M allocation is mixed (`71.702 ms` versus the earlier object-fast `69.912 ms`), so this is not a focused allocation win. It modestly helps application gates: page-token checked Rift `25.007 -> 24.618 ms`, StreamFlex-design checked stream `22.81 -> 22.55 s`, Common Crawl-shaped q2 checked Rift `11.49 -> 11.39 s`. |
| Per-allocation current-slab zeroed flag load | `scalanative_rift_region` now caches whether the current slab is zeroed when a slab is opened, appended, or reset. Object allocation reads the cached region field before deciding whether `_platform_memset` is required, preserving zeroing semantics while avoiding a current-slab flag load on every object. | Runtime checked tests pass. Focused 5M checked Rift allocation improves from the earlier object-fast `69.912 ms` and cached-stats `71.702 ms` to `68.998 ms`, same checksum and zero GC. Focused page-token checked Rift improves `24.618 -> 23.930 ms`; StreamFlex-design checked stream improves `22.55 -> 22.31 s`; generated Common Crawl-shaped q2 checked Rift improves `11.39 -> 11.17 s`. |
| Stats-disabled managed-object allocation body in final-clean Rift regions | `scalanative_rift_region_alloc` and `scalanative_rift_region_alloc_nozero` now use a no-stats inline object-allocation helper when the region-local `alloc_stats_enabled` flag is false. The stats-enabled diagnostic path remains intact. | Runtime checked tests pass (`152/152`). Focused final-clean 5M `ObjectAllocationLoweringMatrix` `rift-checked-rift` improves from the previous documented current-slab-zeroed row `68.998 ms` to `64.485 ms`, same checksum and zero GC. StreamFlexDesign 20M x3 L1 transfer row is heap `32.71 s`, checked Rift stream `19.63 s`, and checked scoped `26.04 s`, same checksum/output. A first stats-enabled trial exposed extra branch cost; the implementation was refined to read the stats flag once, and only final-clean rows are accepted as evidence for this change. |
| Internal region-op timing calls in final-clean Rift regions | Region open/close/reset and slow-allocation duration accounting now call `scalanative_rift_now_ns()` only when the region-local `alloc_stats_enabled` flag is true. L2/stat rows still collect timings; `RIFT_FINAL_CLEAN=1` avoids the internal clock calls. | Validation passes: sandbox compile, compiler suite `337/337`, runtime checked suite `158/158`. StreamFlexDesign checked stream 20M x3 improves to `19.01 s` from the previous post-fast-path `19.63 s`, with RSS `12.5 MB` and matching checksum/output. The post-guard L4 profile has no sampled `mach_absolute_time`, `scalanative_rift_now_ns`, or duration-accounting frames; remaining samples are query mutator `467.80/s`, region allocation/init `244.20/s`, traversal/capsule `30.20/s`. Dataflow L2 sanity still reports region counters, so the change only removes timing work from final-clean rows. |
| Proof-gated no-zero for definitely initialized record objects | The Scala Native lowerer may call `scalanative_rift_region_alloc_nozero` for `RiftOpenStreamingHandle` class allocations only when the local NIR proof shows a concrete/non-module/no-subclass record, all fields are stored before first use/control-flow exit/non-pure operation, and the backend is Rift-owned. The proof now covers primitive-field records and definitely initialized reference-field records; unsafe no-zero remains a lower-bound control. | Focused primitive 5M/10M rows improve normal checked open-handle allocation to the unsafe no-zero ceiling (`65.528 ms` at 5M, `139.832 ms` at 10M). A 2026-05-16 5M reference-record closure rerun keeps normal checked open-handle allocation at the ceiling (`67.109 ms`, `0` zeroed objects, `5,000,001` zero-skipped objects) with matching checksum. The 2026-05-20 runtime allocation-stat probe `openHandleNoZeroSkipsDefinitelyInitializedInferredRecord` proves ordinary inferred `new` under `epochOpenHandle` also reaches the no-zero path for a reference-field record (`zeroSkipped >= 2`, `zeroed == 0`). Current validation passes compiler `350/350`, runtime checked `160/160`, and sandbox compile. |
| Accidental DSPBench parser/query tuple allocation | `DSPBenchRegionMatrix` now uses reusable scratch records for file-backed parser outputs and reusable result fields for `FraudPredictorState.update` / `LogStatusState.update`, instead of returning per-record tuples. | Backend-neutral mutator-parity cleanup. Fresh native-link validation passed; generated 20k smokes matched checksum/output for Fraud q2 and Log q2 across heap, checked page-token, checked epoch stream, and checked epoch scoped. File-backed 20k smokes matched for heap and checked page-token. Post-fix L4 profiles under `/Users/siyaoliu/rift/cache/profile-sweep-20260519-dspbench-postscratch-linked` contain no sampled `Tuple` frames in the DSPBench state-update path. Generated 1M x3 post-fix rows are under `/Users/siyaoliu/rift/cache/dspbench-mutator-parity-generated-1m-20260519-linked`: Fraud q2 heap `447.066 ms`, checked epoch stream/scoped `305.767/305.361 ms`; Log q2 heap `417.961 ms`, checked epoch stream/scoped `273.882/273.639 ms`. Page-token checked is mixed (`486.294 ms` Fraud, `385.719 ms` Log), so direct epoch remains the better checked topology for these generated q2 controls. |
| DSPBench checked page-token explicit allocation source plumbing | The file-backed Fraud/Log checked page-token rows still used explicit `RiftAllocator.allocateOpenHandle(currentHandle, new CheckedRecord(...))` in the Rift-backed page-token append path. This was source/API plumbing, not a sampled residual heap-allocation failure. | `DSPBenchRegionMatrix` now has `rift-checked-page-token-inferred` / `checked-region-stream-inferred`, which passes the selected `pageTokenAppendRiftOpenHandleFor` owner as a method parameter, constructs `CheckedRecord` with ordinary `new` under that active owner, widens to the parent stream owner, and appends through the same page-token API. Existing explicit checked page-token, open-region legacy, and checked scoped controls remain available. Sandbox compile and native-link smoke passed. Focused 20k real file-backed Fraud/Log smokes matched heap/explicit/inferred checksum/output; inferred preserved explicit region-object counts (`80111` Fraud, `60000` Log). No L1/L2 elapsed or L4 bucket reduction is claimed. |
| Accidental Wikimedia checked session boxed loop state | The checked Wikimedia session body captured outer mutable `checksum`, `processed`, `group`, and `done` through the region callback, showing up as `scala.runtime.LongRef`, `BooleanRef`, and `IntRef` in L4 top frames. The later inferred split still had mutable loop state outside the open-handle callback. | `LogHubRetainedSessionMatrix` now uses immutable base values and local callback state in the checked group body, keeps parser output in a reusable scratch object, and region-allocates the per-group checked `counts` array. 20k smoke and 1M L2 rows matched checksum/output. The latest state-local L4 profile under `/Users/siyaoliu/rift/cache/profile-sweep-20260520-wikimedia-inline-reset-state-escalated` no longer shows boxed-ref checked top-frame parameters, region allocation remains tiny, and query/session-loop samples stay near the split-inferred row. This is accepted as mutator-parity cleanup, not a new headline speed claim. |
| Shared Wikimedia clickstream multi-pass TSV/hash parser | The Wikimedia retained clickstream profile showed the old parser walking each byte line multiple times: `tsvFieldHash` for source/target/link-kind fields, `tsvFieldInt` for count, and standalone whole-line `stableHash`. This was a shared parser/input floor, not checked-region allocation overhead. | `LogHubRetainedSessionMatrix.readClickstreamFieldsInto` now computes the three field FNV hashes, count, and whole-line FNV hash in one byte pass, and the unused old TSV helper methods were removed. This is backend-neutral: heap, explicit checked, inferred checked, and scoped rows all use the same parser. Compile/native link passed; 20k compressed smokes matched all four modes. The 1M x3 L2 gate under `/Users/siyaoliu/rift/cache/loghub-wikimedia-fused-parser-1m-20260520` is heap `1219.250 ms`, explicit checked `1203.342 ms`, inferred checked `1124.139 ms`, and scoped `1216.653 ms`, all with checksum `250002331971566003` and output `922453`. L4 under `/Users/siyaoliu/rift/cache/profile-sweep-20260520-wikimedia-fused-parser` no longer samples the old separate TSV/hash helpers. This is accepted as shared mutator-parity/fairness cleanup, not a Rift-specific memory-management win. |
| Shared ByteLineReader per-byte line copy | Post-array-source L4 profiles still sampled `BenchmarkInputSupport.ByteLineReader.nextByte` and `append` as shared input work in Wikimedia/LogHub rows. This was a parser/input floor shared by heap and checked modes. | `ByteLineReader.readLine` now scans each filled input buffer for newline bytes and copies contiguous spans into the reusable line buffer with `System.arraycopy`, preserving CR trimming and EOF final-line semantics. Sandbox compile passed. Smokes matched checksum/output for compressed Wikimedia, archive-backed HDFS join, and zip-backed Theodolite retained UC4. The 1M x3 L2 gates are Wikimedia heap `1061.907 ms`, explicit checked `1041.414 ms`, inferred checked `960.242 ms`, scoped `1047.725 ms` under `/Users/siyaoliu/rift/cache/loghub-bytereader-wikimedia-1m-l2-20260521`; HDFS join heap `7134.073 ms`, explicit checked `7138.545 ms`, inferred checked `7097.635 ms`, scoped `7140.815 ms` under `/Users/siyaoliu/rift/cache/loghub-bytereader-join-1m-l2-20260521`. Focused L4 under `/Users/siyaoliu/rift/cache/profile-sweep-20260521-bytereader-loghub` shows the old `nextByte`/`append` top frames are gone, but the coarse parser/input/hash bucket is mixed rather than uniformly lower. This is shared input fairness cleanup, not Rift-specific region overhead removal. |
| Shared Theodolite byte-field/numeric parser | The retained UC4 L4 profiles remained dominated by UCI byte-line parsing and decimal extraction after checked loop-state cleanup; the old parser used a reusable `DelimitedByteFields` cursor plus repeated advance/read-current calls before numeric parsing. | `TheodolitePowerRegionMatrix.parseCurrentLine` now uses a direct byte cursor to locate fields `2`, `4`, `6`, `7`, and `8`, then calls the existing byte numeric parsers. A fully fused numeric parser variant was tried and rejected because it worsened the 1M gate. Sandbox compile passed; 20k heap/checked stream/checked scoped smokes matched checksum `-2895454912458695581` and output `6176`. The accepted 1M x3 L2 gate under `/Users/siyaoliu/rift/cache/theodolite-bytecursor-parser-1m-l2-20260520` is heap `2325.524 ms`, checked stream `1965.787 ms`, checked scoped `1999.749 ms`, all checksum `5496025699187626461` and output `61760`. L4 under `/Users/siyaoliu/rift/cache/profile-sweep-20260520-theodolite-bytecursor-parser` drops checked parser/input from the prior `168.60/s` to `147.00/s`. This is shared parser/input fairness cleanup, not Rift-specific region overhead removal. |
| Theodolite checked active-handle explicit allocation source plumbing | After active open-handle ordinary-`new` inference was proven, the retained-UC4 checked handle paths still spelled retained measurement/contribution construction as explicit `RiftAllocator.allocateOpenHandle(region, new ...)`. | `TheodolitePowerRegionMatrix` now constructs `CheckedMeasurement` and `CheckedContribution` with ordinary `new` under the active `resetOpenHandle` owner in both streaming-file and preloaded handle paths. Scoped/open-region and legacy explicit-allocation controls remain unchanged. Sandbox compile passed, and a focused 20k real archive-member retained-UC4 smoke matched heap/checked stream/checked scoped checksum `-2895454912458695581` and output `1544`; checked stream reported `260000` region objects and `0.556 ms` Rift op time. This is source-use/inference coverage only; no L1/L2 elapsed or L4 bucket reduction is claimed. |
| Inferred join enclosing inline-wrapper shape | The generic inferred join path used the same enclosing inline-wrapper shape that the minimized inline-reset probe showed capture checking cannot preserve reliably. | `checked-rift-inferred` join is now split out to the sandbox-only inline reset bridge, while explicit `checked-rift` join remains on the old helper for comparison. Archive-backed HDFS 20k smoke matched checksum/output across heap, explicit checked, inferred checked, and scoped. At 1M x3 L2, inferred checked join is `8350.524 ms` versus heap `8268.847 ms`, explicit checked `10102.728 ms`, and scoped `8389.309 ms`, all with matching checksum/output and low checked RSS. L4 follow-up shows the inferred join is still dominated by parser/input/hash (`622.60` samples/sec) and safepoint polling (`189.80` samples/sec), with no checked mark/sweep metadata bucket; the new callback-ref-shape bucket is `12.60` samples/sec, marking generated closure bodies whose signatures carry `scala.runtime.*Ref`. This is accepted as source-shape cleanup/control evidence; heap GC is too small for a memory-management win. |
| StreamFlexDesign checked throughput callback-ref source shape | The callback-ref classifier showed `211.00` samples/sec in generated checked stream callback bodies before source cleanup. | The checked throughput loop now keeps checksum/output/drop counters inside the `streamingOpenHandle` callback and passes immutable per-period values into reset bodies. Sandbox compile and 20k smokes for `checked-epoch-stream` and `checked-epoch-stream-inferred` passed with matching checksum/output. After correcting the profile helper to classify `StreamFlexDesignMatrixHelpers.*anonfun` as query body work, L4 drops callback-ref samples from `211.00/s` to `0.00/s`; actual traversal/capsule remains small at `35.40/s`, region allocation/init is `273.60/s`, and query/body work is `491.60/s`. A 20M x3 L1 row is heap `34.58 s`, checked stream `19.98 s`; the prior checked stream checkpoint was `19.63 s`. This is accepted only as source-shape/profile-clarity cleanup, not a speed optimization. |
| Dataflow direct-epoch inferred allocation source shape | `DataflowRegionMatrix` direct epoch modes now have an inferred active-handle path that uses ordinary `new` where the captured expected type proves the active owner. | A 20k aggregate smoke matched checksum/output across heap, explicit direct epoch, inferred direct epoch, and scoped direct epoch; inferred SELECT/JOIN smokes also passed. The focused 20M x3 L2 gate under `/Users/siyaoliu/rift/cache/dataflow-inferred-direct-epoch-20m-20260520` matched checksum `2584512318695`: heap `755.885 ms` with `177.424 ms` median GC; explicit checked direct epoch `507.004 ms`, `2.724 ms` region op, and `21310740` region objects; inferred checked direct epoch `493.087 ms`, `1.863 ms` region op, and the same object count; checked scoped direct epoch `457.737 ms`. Because L2 counts Rift allocator stats for direct Rift but not scoped, final-clean timing is the backend-selection source: the sequential 20 x 5M docs x3 L1 row under `/Users/siyaoliu/rift/cache/dataflow-inferred-direct-epoch-finalclean-100m-seq-20260520` is heap `12.83 s`, RSS `2289 MB`; explicit checked direct epoch `6.43 s`, RSS `168 MB`; inferred checked direct epoch `6.41 s`, RSS `168 MB`; checked scoped direct epoch `6.33 s`, RSS `168 MB`. This is accepted as inference/source-plumbing cleanup for Dataflow, with the remaining direct-vs-scoped gap now about `1-2%` in final-clean timing. |
| StreamFlexDesign inferred allocation source shape | The StreamFlex checked stream path has both explicit `RiftAllocator.allocateOpenHandle` and inferred ordinary-`new` variants under the same active open-handle owner. | After the final-clean timing guard, the 20M x3 L1 inferred checked stream row is `18.16 s`, RSS `12.5 MB`, versus explicit checked stream `19.01 s`, same checksum/output. L4 buckets remain similar to explicit checked stream: query mutator `464.40/s`, region allocation/init `251.00/s`, traversal/capsule `34.80/s`, other `22.40/s`. This is accepted as a ReML-style inferred-source speedup, but the next StreamFlex bottleneck is still allocation body/object construction plus query pipeline work, not residual explicit allocation plumbing. |
| Throughput-biased reusable-slab policy on large direct epochs | `RIFT_REGION_REUSE_POLICY=cache-large` retains a larger bounded regular-slab pool, avoiding repeated remap/unmap when one epoch's slab working set exceeds the default retention budget. | Dataflow inferred direct epoch at 20 x 5M docs x3 shows default `6.68/6.44 s` versus `cache-large` `6.25/6.24 s`, with the same `167804928` RSS and matching checksum. L2 counters explain the win: default maps `17301` slabs and `587546624` bytes with `55.654 ms` region-op; `cache-large` maps `20` slabs and `21299200` bytes with `9.627 ms` region-op. `prezero-large` maps only `20` slabs but raises region-op to `60.471 ms`, so it remains rejected. This is an opt-in throughput/RSS tradeoff, not the default policy and not a lower-memory claim. |
| Accidental Theodolite checked stream boxed loop state | The checked retained-UC4 stream body captured outer mutable `checksum`, `index`, `length`, and epoch counters through the region callback, showing up as `scala.runtime.IntRef` and `LongRef` in L4 top frames. | `TheodolitePowerRegionMatrix` now uses local primitive accumulators inside the checked epoch body and returns a primitive-only `StreamingEpochOutcome` after close. 20k smoke and 1M L2 follow-up rows matched checksum/output. The checked Rift 1M L2 row improved from the earlier `2143.809 ms` to `2049.682 ms`; post-fix L4 under `/Users/siyaoliu/rift/cache/profile-sweep-20260520-theodolite-loopshape` no longer shows boxed-ref checked top-frame parameters. This is accepted as mutator-parity cleanup, not a region-inference milestone. |
| Accidental Broom checked callback boxed loop state | The q17/shopper checked reset bodies read mutable outer `checksum`, `processed`, and `group`, showing up as `LongRef`/`IntRef` callback-ref-shaped frames in the 2026-05-19 profile (`255.80/s` q17, `366.80/s` shopper). | `BroomRetainedDataflowMatrix` now captures immutable per-group base values in checked reset bodies. Sandbox compile passed; 20k heap/checked smokes matched checksum/output for q17, shopper, aggregate, and join. Focused L4 under `/Users/siyaoliu/rift/cache/profile-sweep-20260520-broom-callback-local` drops q17/shopper `callback_ref_shape` to `0.00/s`. Post-cleanup 20M active-16 rows are recorded under `/Users/siyaoliu/rift/cache/broom-callback-local-20m-l1-rss-20260520` and `/Users/siyaoliu/rift/cache/broom-callback-local-20m-l2-20260520`: q17 checked Rift is `9.45 s` L1 / `3229.305 ms` L2 versus heap `13.17 s` / `4782.648 ms`, and shopper checked Rift is `14.74 s` L1 / `5115.933 ms` L2 versus heap `17.55 s` / `6285.094 ms`. This is accepted as profile-clarity/mutator-parity cleanup; the isolated source-shape effect is the profile bucket removal, not a standalone speed claim. |
| Explicit Broom retained-record and generated-array allocator plumbing | Aggregate/join already had `checked-rift-inferred` direct `new` rows, but q17/shopper still used explicit `RiftAllocator.allocateOpenHandle(new ...)` for high-frequency retained records even though the active owner was statically available. A follow-up source audit found generated Broom inferred paths still had explicit active-handle `new Array` plumbing for per-group object arrays. | `checked-rift-inferred` now covers q17 `CheckedQ17Part` / `CheckedQ17PartEntry` / `CheckedQ17LineItem`, shopper view/cart/purchase/candidate nodes, and generated per-group object arrays in aggregate (`heads`/`tails`/`tables`), join (`left`/`right`), q17 (`tables`), and shopper (`views`/`carts`/`purchases`/`candidates`). TPC-H file-input q17 conservatively falls back to the explicit checked path. Sandbox compile/native link passed; 20k aggregate/join/q17/shopper smokes matched heap/explicit/inferred checksum/output. The 20M q17/shopper record gate preserves region allocation counts/RSS, and the 1M generated-array L2 gate under `/Users/siyaoliu/rift/cache/broom-inferred-arrays-1m-l2-20260521` also preserves identical explicit/inferred region objects: aggregate `1839798`, join `1000006`, q17 `1478215`, shopper `1142743`. No new L4 bucket reduction is claimed; this is accepted as ReML-style source-placement expansion, not as a speed win. |
| Selected local direct-allocation alias heap fallback | Direct branch/match construction was covered when the `new` expressions appeared directly in the constrained expression, but the common helper shape `val first = new T(...); val second = new T(...); val selected = if flag then first else second; selected` lost the original allocation symbols. This is a general ReML-style local flow shape for ordinary records, not a benchmark-local rewrite. | `RiftRegionInference` now records immutable local aliases that select between already-known direct allocation locals and applies later method-return, owner-token method-argument, or framework owner-token constraints to each original allocation. The selected alias itself is marked as a proven region value once every candidate has the same owner, so `RiftRegion.prependRegionList(region, list, selected)`, `RiftRegion.append(region, objectBuffer, selected)`, `region.append(regionBuffer, selected)`, `RegionPriorityQueue.push`, `RegionIndexedPriorityQueue.put`, and `RegionLongIndexedPriorityQueue.put` pass framework store checking. Runtime allocation stats prove both selected candidate objects allocate in checked region memory, including the plain priority-queue push path; compiler negatives reject unrooted heap metadata stored by either candidate. Validation passed with compiler `426/426`, runtime `190/190`, `sandbox3_next` compile, and a checksum-matching ReML `ratio` smoke. No L1/L2 elapsed or L4 bucket reduction is claimed until a benchmark path samples this exact source shape. |
| Selected local synthetic-factory alias heap fallback | The selected-local alias proof had been exercised for plain direct object records, while common synthetic wrappers such as `Some(new T(...))`, `Option(new T(...))`, and `Tuple2(new A(...), new B(...))` could still regress silently if the direct-region-construct path stopped preserving aliases through method summaries, owner-token call boundaries, region-owned array stores, checked framework buffers, or checked priority queues. | `ObjectBuffer`, `RegionBuffer`, `RegionPriorityQueue`, `RegionIndexedPriorityQueue`, and `RegionLongIndexedPriorityQueue` now accept captured object element types (`T <: Object^`) while their append/push/put/get APIs still require the explicit owner-token value type `T^{owner}`. Compiler positives cover explicit-owner methods returning selected local `Some`/`Option.apply`/`Tuple2` factory values, owner-token methods consuming them, region-owned arrays storing them, checked `ObjectBuffer`/`RegionBuffer` appends storing them, ordinary priority-queue push/put calls storing them, and dense/long lexicographic indexed priority-queue `put` overloads storing them. Compiler negatives reject unrooted heap metadata through selected `Some`/`Option.apply`/`Tuple2` buffer and queue operations and preserve inner-to-outer rejection. Runtime allocation stats prove selected buffer wrappers plus nested payloads allocate in checked region memory (`delta >= 28`), selected ordinary priority-queue wrappers plus payloads allocate in checked region memory (`delta >= 14`), and selected lexicographic priority-queue wrappers plus payloads allocate in checked region memory (`delta >= 14`). Validation passed with compiler `457/457`, runtime `196/196`, `sandbox3_next` compile, and a checksum-matching ReML `ratio` smoke across heap, inferred checked stream, and checked scoped rows. This is a library/inference coverage slice; no L1/L2 elapsed or L4 bucket reduction is claimed. |
| Stream-window rank/table-rank selected synthetic owner-token heap fallback | The selected `Some`/`Option.apply`/`Tuple2` aliases above still could not flow through checked stream-window rank/table-rank APIs: rank/table wrappers accepted only uncaptured `Object`, and the inference pass deliberately excluded parent `StreamingRegion` from generic allocation ownership. | `StreamWindowIndexedRank`, `StreamWindowLongIndexedRank`, and `StreamWindowTableRank` now accept captured object element types (`T <: Object^`), and `RiftRegionInference` recognizes `putWindowRank`, `putWindowRankInBucket`, and `putTableRankInBucket` only when a checked rank/table context supplies the stream owner token and every selected alias candidate is already proven placeable. Compiler negatives reject unrooted heap metadata through all three boundaries, while existing direct heap-object stream-rank negatives keep parent `StreamingRegion` from becoming a generic owner. Runtime allocation stats prove the selected wrappers plus nested payloads allocate in checked region memory (`delta >= 14`, measured after close/reset stat flush). Validation passed with compiler `461/461`, runtime `197/197`, `sandbox3_next` compile, and a checksum-matching ReML `ratio` smoke. This removes one accidental synthetic heap fallback in checked framework paths; no elapsed or L4 bucket reduction is claimed yet. |
| Stream-window rank/table-rank branch/match-local direct-allocation heap fallback | After selected aliases were covered, non-direct local-selection shapes such as `put...(if flag then first else second, ...)` and `put...(selector match { case 0 => first; case _ => second }, ...)` still looked like unrooted heap values even when every selected value was an immutable local direct allocation and the checked rank/table API supplied the owner token. | `RiftRegionInference` now constrains branch/match-local direct allocation candidates at checked stream rank/table APIs, and `NirGenExpr` treats `if`, `match`, `Return`, and Scala 3 lowered `Labeled` match-result expressions as known region values only when every returned value is known region-local. Compiler positives cover `putWindowRank`, `putWindowRankInBucket`, and `putTableRankInBucket`; negatives reject unrooted heap metadata through selected candidates; runtime stats prove six branch/match rank/table candidates allocate in checked region memory (`delta >= 6`). Validation passed with compiler `468/468`, runtime `198/198`, `sandbox3_next` compile, and a checksum-matching ReML `ratio` smoke. Direct local heap-looking rank/table values remain rejected, and no elapsed or L4 bucket reduction is claimed. |
| Owner-token direct branch/match synthetic-factory heap fallback | Direct branch/match factory expressions such as `consume(using r)(if flag then Some(new T(...)) else Some(new T(...)))`, checked buffer/priority-queue stores, or checked rank/table `put...(selector match { case 0 => Option(new T(...)); case _ => Option(new T(...)) })` were not covered unless each factory result was first named in a selected local alias. | `RiftRegionInference` now walks direct branch/match returned region constructs at owner-token boundaries, and `NirGenExpr` counts attached direct factory apps as known region values for branch/match store checking. Compiler positives cover explicit owner-token method arguments, checked `ObjectBuffer`/`RegionBuffer` appends, ordinary checked priority-queue push/put calls, dense/long lexicographic checked priority-queue put overloads, and checked stream rank/table APIs for direct branch/match `Some(new T(...))`, `Option(new T(...))`, and `Tuple2(new A(...), new B(...))`; negatives reject unrooted heap metadata through the same path, including buffer and queue stores. Runtime stats prove the executed wrappers plus payloads allocate in checked region memory at explicit owner-token method arguments, checked buffer appends, ordinary checked priority queues, and checked stream rank/table APIs (`delta >= 7`). Validation passed with compiler `479/479`, runtime `202/202`, `sandbox3_next` compile, and a checksum-matching ReML `ratio` smoke. This removes another accidental synthetic heap fallback in checked owner-token paths; no elapsed or L4 bucket reduction is claimed yet. |
| RegionList branch/match nested synthetic-factory heap fallback and payload safety | Checked `RegionList` already had local/direct/selected direct-object placement, but its element bound was still uncaptured and direct branch/match-created list nodes with nested synthetic factory fields were not explicitly proven. The new proof also exposed that an attached outer region-owned constructor could hide unrooted heap metadata inside a nested direct factory argument. | `RegionList` now accepts captured node element types (`T <: RegionListNode^`) while `prependRegionList` still requires `T^{owner}`. `NirGenExpr` now recursively validates payloads of direct region-placed constructors/factories under branch/match/lowered-match forms, so `Option(metadata)` or `Tuple2(metadata, ...)` nested in a region-owned list node is rejected. Compiler positives cover RegionList nodes containing branch/match `Some(new T(...))`, `Option(new T(...))`, and `Tuple2(new A(...), new B(...))`; negatives cover nested unrooted metadata. Runtime stats prove executed nodes, wrappers, and payloads allocate in checked region memory (`delta >= 10`). Validation passed with compiler `482/482`, runtime `203/203`, `sandbox3_next` compile, and a checksum-matching ReML `ratio` smoke. This is an inference/safety coverage slice; no elapsed or L4 bucket reduction is claimed. |
| RegionList selected-nested synthetic-factory heap fallback | After branch/match-created RegionList nodes were proven, the helper shape `val selected = if flag then Some(new T(...)) else Some(new T(...)); prependRegionList(region, list, new Node(selected))` still had no explicit proof. A naive nested-local inference would also make straight aliases such as `val tag = metadata` unsafe. | `RiftRegionInference` now walks inside already constrained constructors/factories and constrains nested local aliases only when they are multi-candidate selected allocation aliases. Compiler positives cover RegionList node fields consuming selected local `Some`/`Option.apply`/`Tuple2` factory aliases; negatives reject selected `Option(metadata)` and preserve older straight-alias metadata rejections. Runtime stats prove executed nodes, selected wrappers, and payloads allocate in checked region memory (`delta >= 17`). Validation passed with compiler `484/484`, runtime `204/204`, `sandbox3_next` compile, and a checksum-matching ReML `ratio` smoke. This is an inference/safety coverage slice; no elapsed or L4 bucket reduction is claimed. |
| Checked framework selected-nested synthetic-factory heap fallback | The selected-nested helper was proven for `RegionList`, but the same compiler helper also reaches other owner-token framework calls. Without explicit tests, `val selected = if flag then Some(new T(...)) else Some(new T(...)); append(region, buffer, new Node(selected))` and priority-queue `push/put(..., new Node(selected), ...)` could regress or accidentally infer straight heap aliases. | Compiler positives now cover `ObjectBuffer`/`RegionBuffer` appends and ordinary `RegionPriorityQueue`/`RegionIndexedPriorityQueue`/`RegionLongIndexedPriorityQueue` push/put calls where the containing value object stores selected local `Some`/`Option.apply`/`Tuple2` factory aliases. Compiler negatives reject selected nested `Option(metadata)`/`Tuple2(metadata, metadata)` through object buffers, region buffers, and priority queues. Runtime stats prove selected wrappers, payloads, and containing value objects allocate in checked region memory for buffers and priority queues (`delta >= 17` each). Validation passed with compiler `489/489`, runtime `206/206`, `sandbox3_next` compile, and a checksum-matching ReML `ratio` smoke. This is an inference/safety coverage slice; no elapsed or L4 bucket reduction is claimed. |
| Reset-open-handle direct array allocation plumbing in inferred LogHub/Wikimedia paths | The inferred Wikimedia session and LogHub join paths had ordinary `new` for retained records but still used explicit `RiftAllocator.allocateOpenHandle(region, new Array(...))` for per-group checked arrays inside the inline reset callback. This was remaining source-level allocation plumbing, not sampled region-op overhead. | Compiler/runtime tests now prove direct `new Array` placement inside `resetOpenHandleInline` for both captured object arrays and primitive `Array[Int]^{region}`; the negative rejects unrooted heap metadata stores. `LogHubRetainedSessionMatrix` inferred session/join now use ordinary `new Array` for the per-group arrays, while explicit checked rows remain controls. Validation passed with compiler `491/491`, runtime `207/207`, `sandbox3_next` compile, and 20k compressed Wikimedia/HDFS smokes matching heap/explicit/inferred/scoped checksum and output. The 1M x3 L2 gates also matched: Wikimedia heap `1383.161 ms`, explicit checked `1341.892 ms`, inferred checked `1254.338 ms`, scoped checked `1344.536 ms`, with explicit/inferred region objects both `1922465`; HDFS join heap `8031.714 ms`, explicit checked `7839.871 ms`, inferred checked `7772.096 ms`, scoped checked `7737.018 ms`, with explicit/inferred region objects both `1000006`. Focused L4 under `/Users/siyaoliu/rift/cache/profile-sweep-20260521-loghub-array-source` shows Wikimedia inferred checked lower than explicit checked in parser/input/hash (`477.00/s` versus `501.20/s`) and query/session-loop (`80.80/s` versus `118.20/s`), while region allocation remains tiny (`4.40/s`) and callback-ref samples remain `0.00/s`; HDFS join remains parser/input/hash dominated and short-run/idle-contaminated. A 2026-05-21 07:56 source audit found the inferred Wikimedia session arrays had lagged the evidence; `entries`, `heads`, `tails`, and `counts` now use ordinary `new Array` too. Fresh compile/native-link and compressed Wikimedia gates passed: 20k smoke checksum `4260216346575211415`, output `18980`; 1M x3 L2 heap `1069.051 ms`, explicit checked `1052.706 ms`, inferred checked `965.723 ms`, scoped checked `1053.033 ms`, with explicit/inferred region objects both `1922465`. This is a ReML-style source-placement cleanup validated at L2 plus diagnostic L4, not evidence of reduced region-object count. |
| Owner-token direct array argument heap fallback | Direct `new Array` arguments to owner-token methods, such as `consume(using r)(new Array[T^{r}](n))`, could type-check while the source array construction was lowered to the runtime array factory before GenNIR consumed the checked owner proof. This left a general framework/API source shape less complete than the reset-open-handle array path. | `RiftRegionInference` now records direct owner-token method-argument construct owners by source span, and `NirGenExpr` uses that bridge when the lowered runtime array factory is generated. Compiler positives cover direct owner-token array arguments and compiler negatives reject unrooted heap stores through the captured array parameter. Runtime allocation stats prove the direct array argument and inline stored region values allocate in checked region memory (`delta >= 3`). Validation passed with compiler `504/504`, runtime `215/215`, and `sandbox3_next` compile. No L1/L2 elapsed or L4 bucket reduction is claimed for this proof slice. |
| Checked `ObjectBuffer`/`RegionBuffer`/priority-queue direct array append/get/peek heap fallback | The owner-token array proof did not yet cover checked framework stores where `RiftRegion.append(region, objectBuffer, new Array[T^{region}](n))`, `region.append(regionBuffer, new Array[T^{region}](n))`, or checked priority-queue `push`/`put` sees the source array after Scala lowers it to `scala.runtime.Arrays.newArray`; checked `get`/`peek` results can also erase the recovered local array shape before later element stores. | `RiftRegionInference` now records direct checked-buffer append and priority-queue push/put array owners by source span, records checked-buffer `get` and checked priority-queue `peek`/`pop`/`get` locals as owner-region values, and remembers recovered array element owners only when the element type itself captures the checked owner. `NirGenExpr` treats runtime array class-token arguments as construction metadata rather than stored payloads, while array element stores remain checked separately. Compiler positives cover appending/pushing/putting and then populating `Array[T^{region}]^{region}` through `ObjectBuffer`, `RegionBuffer`, ordinary `RegionPriorityQueue`, `RegionIndexedPriorityQueue`, `RegionLongIndexedPriorityQueue`, and the dense/long-key lexicographic indexed priority-queue overloads; compiler negatives reject unrooted heap stores through recovered `Array[Metadata]^{region}`. Runtime allocation stats prove the appended/pushed/put arrays and inline stored region values allocate in checked region memory (`delta >= 4` ordinary queue; `delta >= 6` indexed plus long-indexed queues; `delta >= 6` lexicographic indexed plus long-indexed queues). Validation passed with compiler `516/516`, runtime `220/220`, and `sandbox3_next` compile. No L1/L2 elapsed or L4 bucket reduction is claimed for this proof slice. |
| Checked stream-rank direct array put/peek heap fallback | The checked framework direct-array proof did not cover `putWindowRank`, `putWindowRankInBucket`, or `putTableRankInBucket` for direct `new Array[T^{stream}]` values, and rank result types can erase the inner array element capture before later element stores. | `RiftRegionInference` now recognizes checked stream-rank/table-rank peek/get result owners, registers framework owner-token parameters for capture-name recovery, remembers direct-array element ownership from the prior checked `put` value type on the same immutable rank/table local, and bridges recovered array element owners to GenNIR. The proof is intentionally bounded to result locals whose recovered or recorded array element type captures the checked stream owner; unannotated direct put/peek locals no longer need explicit captured result-local ascriptions. Compiler positives cover indexed, long-key lexicographic, and table-rank direct arrays plus inline region-value element stores; compiler negatives reject unrooted heap metadata stores through recovered `Array[Metadata]^{stream}`. Runtime stats prove the inserted arrays and stored leaves allocate in checked region memory (`delta >= 6`). Validation passed with compiler `520/520`, runtime `221/221`, and `sandbox3_next` compile at 2026-05-21 11:00 CEST. No L1/L2 elapsed or L4 bucket reduction is claimed for this proof slice. |
| Region-owned array branch/match direct-factory heap fallback | Region-owned array stores covered inline direct `new`, inline `Some`/`Option.apply`/`Tuple2`, and selected local synthetic aliases, but a direct `if`/`match` store such as `items(i) = if flag then Some(new T(...)) else Some(new T(...))` did not have an explicit inference-decision proof for every returned factory site. | `RiftRegionInference` now walks `if`/`match` returned direct constructors/factories at region-owned array stores and records the same checked array element owner on each returned allocation site. Compiler positives cover branch/match direct object, `Option.apply`, and `Tuple2` stores; a compiler negative rejects branch-returned `Some(metadata)` with unrooted heap metadata. Runtime stats prove the executed arrays, wrappers, and payloads allocate in checked region memory (`delta >= 9`). Validation passed with compiler `522/522`, runtime `222/222`, and `sandbox3_next` compile at 2026-05-21 11:06-11:08 CEST. No L1/L2 elapsed or L4 bucket reduction is claimed for this proof slice. |
| Epoch-fold child-region explicit allocation plumbing | `epochFoldRegionFor` returned a checked child region but was not in the inference pass's selected child-region factory list, so epoch-fold records still needed explicit `RiftRegion.alloc(new ...)(using region)` to prove placement. `DataflowRegionMatrix` still carried that explicit source plumbing in its generic aggregate epoch-fold control. | `RiftRegionInference` now treats `epochFoldRegionFor` as a checked child-region owner helper. Compiler positives/negatives cover ordinary `new` epoch-fold records and unrooted heap metadata rejection; runtime stats prove two records allocate in checked region memory (`delta >= 2`). `DataflowRegionMatrix.runCheckedAggregateEpochFold` now uses ordinary `new CheckedAggregateEvent(...)` under the returned fold child owner and widens to the parent stream owner before `putEpochFold`. Validation passed with compiler `524/524`, runtime `223/223`, and `sandbox3_next` compile at 2026-05-21 11:14-11:16 CEST; the source-use follow-up passed `sandbox3_next` compile and a focused 20k Dataflow aggregate heap/checked epoch-fold smoke at 2026-05-21 11:22-11:27 CEST with checksum `3276431580` and checked `20004` region objects. No L1/L2 elapsed or L4 bucket reduction is claimed for this proof slice, and generic epoch-fold remains speed-gated. |
| Method-returned local closure heap fallback | The direct method-returned closure slice placed direct returned closures, but `{ val f = (n: Int) => ...; f }` still needed an explicit captured type ascription on `f` or the closure object/body allocation stayed on the heap. This is a frequent synthetic/helper allocation shape in the ReML inference roadmap, not a benchmark-local rewrite. | `RiftRegionInference` now records immutable local closure allocation sites and constrains returned local closures from explicit checked-owner methods through the captured method result owner. Runtime allocation stats prove both the closure wrapper and the body-returned object allocate in checked region memory; compiler negatives still reject unrooted heap metadata captured by the returned local closure body. Validation passed with compiler `398/398`, runtime `178/178`, `sandbox3_next` compile, and a checksum-matching ReML `ratio` native smoke. No L1/L2 elapsed or L4 bucket reduction is claimed until a benchmark path samples this exact source shape. |
| Owner-token local closure argument heap fallback | Inline closure arguments were owner-token inferred, but the named-local helper form `val f = (n: Int) => ...; consume(using r)(f)` could miss checked placement unless `f` had an explicit captured type ascription. This is a general framework/API source shape, not a benchmark-local rewrite. | `RiftRegionInference` now constrains immutable local closure arguments through the owner-token method parameter type and supplied checked owner. Runtime allocation stats prove both the local closure wrapper and the body-returned object allocate in checked region memory when the closure explicitly captures that owner term; compiler negatives reject unrooted heap metadata captured by the closure body. Validation passed with compiler `400/400`, runtime `179/179`, `sandbox3_next` compile, and a checksum-matching ReML `ratio` native smoke. No L1/L2 elapsed or L4 bucket reduction is claimed until a benchmark path samples this exact source shape. |
| Owner-token branch/match inline closure-body heap fallback | Direct inline closure arguments selected by `if` or `match`, e.g. `consume(using r)(if flag then ((n: Int) => ...) else ...)`, needed explicit proof that the captured-owner body allocation was still checked-region placed through control flow. | Existing `RiftRegionInference` branch/match direct-returned-closure walking is now covered by compiler positives/negatives and runtime allocation-stat tests. Runtime proves the selected body-returned object is checked-region allocated; the direct-inline native shape observed only one region object, so wrapper allocation is not claimed when the backend does not materialize a wrapper. Validation passed with compiler `408/408`, runtime `183/183`, and `sandbox3_next` compile. No L1/L2 elapsed or L4 bucket reduction is claimed until a benchmark path samples this exact source shape. |
| Local branch/match inline closure-body heap fallback | Captured local expected types, e.g. `val make: Function1[...]^{region} = if flag then ((n: Int) => ...) else ...`, previously used only direct closure recognition and could miss branch/match inline closure bodies. | `RiftRegionInference.markInferredClosureAllocation` now uses the direct-returned-closure walker, so captured local expected types cover branch/match inline closures as well. Runtime allocation stats prove the selected body-returned object is checked-region allocated when the closure explicitly captures the checked owner term; wrapper allocation is not claimed for this direct-inline native shape. Validation passed with compiler `410/410`, runtime `184/184`, `sandbox3_next` compile, and a checksum-matching ReML `ratio` smoke. No L1/L2 elapsed or L4 bucket reduction is claimed until a benchmark path samples this exact source shape. |
| Owner-token selected local closure alias heap fallback | Branch/match local closure arguments were covered when passed directly, but the common alias shape `val selected = if flag then first else second; consume(using r)(selected)` still lost the original closure allocation symbols and looked like an unrooted heap function value. | `RiftRegionInference` now records immutable local closure-selection aliases and preserves the original closure-local symbols when an owner-token method argument consumes the alias. Runtime allocation stats prove selected closure wrappers plus the selected body-returned object are checked-region allocated; compiler negatives reject unrooted heap metadata captured by either selected closure body. Validation passed with compiler `412/412`, runtime `185/185`, `sandbox3_next` compile, and a checksum-matching ReML `ratio` smoke. No L1/L2 elapsed or L4 bucket reduction is claimed until a benchmark path samples this exact source shape. |
| Method-returned selected local closure alias heap fallback | Method-returned local closures were covered for direct and single immutable-local returns, while owner-token selected aliases were covered at call sites. Explicit checked-owner methods returning `val selected = if flag then first else second; selected` still needed proof that the original local closure allocation symbols survive the alias and method-return summary. | The existing `RiftRegionInference` selected-local alias map now feeds the explicit checked-owner method-return path. Runtime allocation stats prove the returned selected closure wrappers and selected body-returned object allocate in checked region memory; compiler negatives reject unrooted heap metadata captured by either selected closure body. Validation passed with compiler `414/414`, runtime `186/186`, `sandbox3_next` compile, and a checksum-matching ReML `ratio` smoke. No L1/L2 elapsed or L4 bucket reduction is claimed until a benchmark path samples this exact source shape. |
| Owner-token branch/match-local closure argument heap fallback | Branch- or match-selected named local closure arguments, e.g. `consume(using r)(if flag then first else second)` or a `selector match` returning local closures, were not covered by the direct local-closure owner-token slice. This is another general framework/API source shape for reusable helper closures. | `RiftRegionInference` now walks branch/match returned local idents at owner-token argument boundaries and constrains each immutable local closure through the supplied checked owner. Runtime allocation stats prove selected closure wrappers and the selected body-returned object allocate in checked region memory; compiler negatives reject unrooted heap metadata captured by any selected closure body. Validation passed with compiler `404/404`, runtime `181/181`, `sandbox3_next` compile, and a checksum-matching ReML `ratio` native smoke. No L1/L2 elapsed or L4 bucket reduction is claimed until a benchmark path samples this exact source shape. |
| Closure-body named-local returned closure heap fallback | The direct closure-body returned-closure proof placed `base => (value => new T(...))`, but a named local inner closure inside the outer closure body still lowered the inner wrapper to heap allocation unless the compiler preserved the local closure value owner. The first local-wrapper proof required an explicit captured function type on the local closure. This is a closure/effect-summary coverage gap, not a benchmark-local rewrite. | `RiftRegionInference` and GenNIR now preserve the closure value owner for typed and untyped named local closures returned from a region-owned closure body when the enclosing closure result type names an explicit checked owner, a unique runtime owner term is recoverable, and the local closure body captures that owner. Runtime allocation stats prove the outer wrapper, named local inner wrapper, and nested body object allocate in checked-region memory (`delta >= 3`); generated LLVM for the untyped proof shows the inner wrapper no longer calls `scalanative_GC_alloc_small`. Compiler negatives still reject unrooted heap metadata. Validation passed with compiler `542/542`, clean runtime `232/232`, and `sandbox3_next` compile at 2026-05-21 17:22-17:24 CEST. Hidden/type-only owner capture without a runtime handle, escaping returned closures, mutable local closure flow, and broad owner-capturing closure fallback remain heap fallback/rejected; no L1/L2 elapsed or L4 bucket reduction is claimed. |
| Stored closure body heap fallback in checked arrays/buffers/priority queues | Direct inline closures stored into region-owned arrays or checked owner-token buffers could have the closure value constrained, but body-local allocations still needed explicit owner plumbing unless the wrapper/body owner survived the store path. Primitive array update lowering also erased captured element types, so GenNIR could not recover the element owner from the array store tree alone. Ordinary checked priority queues had closure-wrapper proof but no explicit allocation-stat proof that pushed closure bodies retained the captured owner effect. | GenNIR now recognizes checked owner aliases and uses a bounded checked-array fallback: if element capture metadata is erased, a stored direct closure may inherit the already inferred region-owned array object owner only when it captures the same runtime owner. The owner-token append/push paths prepare direct closures before checked framework safety checks. Runtime allocation stats prove ObjectBuffer inline closure bodies (`delta >= 3`), selected RegionBuffer closure bodies (`delta >= 4`), direct region-owned array closure-body stores (`delta >= 3`), and ordinary `RegionPriorityQueue.push` inline/selected closure bodies (`delta >= 4`) allocate in checked-region memory. Compiler negatives still reject stored or pushed closures that capture unrooted heap metadata. Validation passed with compiler `549/549`, clean runtime `237/237`, and `sandbox3_next` compile at 2026-05-21 17:57-18:00 CEST. No L1/L2 elapsed or L4 bucket reduction is claimed. |
| Wrapper-nested closure body heap fallback | Region-owned wrapper constructors could place the wrapper and nested direct `new` arguments, but a direct or selected local closure passed as a constructor/factory argument still risked being treated as a heap closure unless the closure appeared directly at an owner-token boundary. This left the simple helper shape `new Wrapper((n: Int) => new T(...))` or `new Wrapper(selectedClosure)` outside the closure/effect-summary coverage. | `RiftRegionInference` now records closures nested inside direct region-owned constructor/factory arguments, and GenNIR attaches the wrapper's proven owner to those nested closure arguments before constructor safety checks. Runtime allocation stats prove wrapper plus inline closure/body allocation (`delta >= 3`) and wrapper plus selected local closure/body allocation (`delta >= 4`) allocate in checked-region memory. Compiler negatives reject unrooted heap metadata captured by the nested closure body. Validation passed with compiler `552/552`, clean runtime `239/239`, and `sandbox3_next` compile at 2026-05-21 18:15-18:21 CEST. This is a simple-wrapper closure/effect proof, not hidden owner capture or broad escaping-closure inference; no L1/L2 elapsed or L4 bucket reduction is claimed. |
| Method-returned `Some(closure)` body heap fallback | The simple-wrapper closure proof was local to direct wrapper construction. A method that returned a library-created wrapper, such as `Some((n: Int) => new T(...))`, also needed proof that method-return factory summaries preserve both the nested closure object and its captured-owner body allocation across the callee boundary. | The existing method-return factory summary now has compiler/runtime proof for `Option[Function1[Int, T^{r}]^{r}]^{r}` returned as `Some(closure)`, including selected immutable local closure aliases. Runtime allocation stats prove method-returned `Some` plus inline closure/body allocation (`delta >= 3`) and method-returned `Some` plus selected closure/body allocation (`delta >= 4`) allocate in checked-region memory. Compiler negatives reject unrooted heap metadata captured by the nested closure body. Validation passed with compiler `555/555`, clean runtime `241/241`, and `sandbox3_next` compile at 2026-05-21 18:26-18:28 CEST. This is a library-wrapper/method-summary closure-effect proof, not broad escaping-closure inference; no L1/L2 elapsed or L4 bucket reduction is claimed. |
| Owner-token generic-wrapper closure body heap fallback | Method-argument owner-token summaries covered direct values and direct closure arguments, but the wrapper helper shape `consume(using r)(new Wrapper[T^{r}](closure))` needed proof that the nested closure object and its body allocation inherit the owner supplied by the method parameter. | Compiler/runtime tests now cover explicit owner-token method arguments whose parameter type is `Wrapper[T^{r}]^{r}`. Runtime allocation stats prove method-argument wrapper plus inline closure/body allocation (`delta >= 3`) and wrapper plus selected closure/body allocation (`delta >= 4`) allocate in checked-region memory. Compiler negatives reject unrooted heap metadata captured by the nested closure body. Validation passed with compiler `558/558`, clean runtime `243/243`, and `sandbox3_next` compile at 2026-05-21 18:33-18:35 CEST. This is a method-argument simple-wrapper closure/effect proof, not broad escaping-closure inference; no L1/L2 elapsed or L4 bucket reduction is claimed. |
| Closure-body callee-summary heap fallback | Closure-body placement already handled body-local direct `new`, returned inner closures, stored closures, and wrapper-nested closures when the body explicitly captured a checked owner term. The next effect-summary gap was a region-owned closure body that called an explicit checked-region method and returned that callee-allocated result, e.g. `build(value)(using owner): T^{owner}`. | Compiler/runtime tests now prove the closure body can call the explicit checked-region callee and keep both the closure wrapper and callee-allocated result in checked-region memory (`delta >= 2`) when the same runtime owner term is captured. The paired compiler negative rejects unrooted heap metadata flowing through the callee allocation. Validation passed with compiler `560/560`, clean runtime `244/244`, and `sandbox3_next` compile at 2026-05-21 18:39-18:42 CEST. This is a bounded closure method/effect-summary proof, not hidden owner capture, escaping closure inference, topology inference, or an elapsed/profile claim. |
| Closure-body forwarded-callee summary heap fallback | The direct callee proof still covered only one method body. A more ReML-like effect-summary shape is a region-owned closure body calling a simple forwarding wrapper, e.g. `forward(value)(using owner): T^{owner} = build(value)(using owner)`, where the final allocation happens in the forwarded callee. | Compiler/runtime tests now prove the captured-owner closure body can call the forwarding method and keep both the closure wrapper and forwarded callee-allocated result in checked-region memory (`delta >= 2`). The paired compiler negative rejects unrooted heap metadata flowing through the forwarded callee allocation. Validation passed with compiler `562/562`, clean runtime `245/245`, and `sandbox3_next` compile at 2026-05-21 19:20-19:22 CEST. This is one additional method/effect-summary hop, not broad library inference, hidden/type-only owner recovery, escaping closure inference, topology inference, or an elapsed/profile claim. |
| Closure-body branch/match forwarded-callee summary heap fallback | The simple forwarded-callee proof did not yet show control-flow method summaries for closure body effects. A ReML-style summary should preserve the region owner through branch/match wrappers when every path returns a value produced under the same explicit checked owner. | Compiler/runtime tests now prove captured-owner closure bodies can call branch and match forwarding methods and keep the closure wrapper plus executed branch/match callee result in checked-region memory (`delta >= 2`). The paired compiler negative rejects unrooted heap metadata flowing through a branch-forwarded callee allocation. Validation passed with compiler `565/565`, clean runtime `247/247`, and `sandbox3_next` compile at 2026-05-21 19:28-19:30 CEST. This is bounded control-flow method/effect-summary coverage, not virtual dispatch, arbitrary callback/library inference, hidden/type-only owner recovery, topology inference, or an elapsed/profile claim. |
| Closure-body Option/Some callee-summary heap fallback | The branch/match callee proof still returned a plain record. A common library-created residual shape is a checked-region callee returning `Option[T^{owner}]^{owner}` via `Some(new T(...))`, called from a captured-owner closure body. | Compiler/runtime tests now prove the closure body can call that checked-region `Option`/`Some` callee and keep the closure wrapper, `Some` wrapper, and nested payload in checked-region memory (`delta >= 3`). The paired compiler negative rejects unrooted heap metadata hidden behind `Some(metadata)`. Validation passed with compiler `567/567`, clean runtime `248/248`, and `sandbox3_next` compile at 2026-05-21 19:36-19:38 CEST. This is a bounded library-wrapper/effect-summary proof, not broad library inference, primitive boxed-key placement, hidden/type-only owner recovery, topology inference, or an elapsed/profile claim. |
| Closure-body direct synthetic-factory heap fallback | Direct closure-body returned factories such as `Some(new T(...))` and `Tuple2(new A(...), new B(...))` type-checked under the captured-owner closure result type, but GenNIR only prepared returned closures before method-body generation. The first runtime attempt observed only one region object, proving the factory and payload allocations still went to the heap. | GenNIR now prepares direct returned region constructs for methods whose closure-body owner was already inferred, attaching the captured runtime owner to the returned factory and nested direct payloads before lowering. Compiler/runtime tests prove the closure wrapper plus direct `Some` plus payload (`delta >= 3`) and the closure wrapper plus direct `Tuple2` plus two payloads (`delta >= 4`) allocate in checked region memory. Compiler negatives reject `Some(metadata)` and `Tuple2(metadata, metadata)` with unrooted heap metadata. Validation passed with compiler `571/571`, clean runtime `250/250`, and `sandbox3_next` compile at 2026-05-21 19:52-19:54 CEST. This is direct closure-body synthetic-factory effect-summary coverage, not broad library inference, primitive boxed-key placement, hidden/type-only owner recovery, topology inference, or an elapsed/profile claim. |
| Closure-body selected-local synthetic-factory heap fallback | The direct synthetic-factory proof still did not cover ordinary helper code that names `Some(new T(...))` or `Tuple2(new A(...), new B(...))` candidates, selects one through an immutable local alias, and returns the selected alias from a captured-owner closure body. The first runtime attempt again observed only the closure wrapper, proving the selected factory candidates still fell back to heap. | `RiftRegionInference` now records local direct factory apps when selected aliases constrain them, and GenNIR resolves returned immutable aliases in proven closure-body methods back to those direct factory RHSs before lowering. Compiler/runtime tests prove the closure wrapper plus two `Some` candidates and two payloads (`delta >= 5`) and the closure wrapper plus two `Tuple2` candidates and four payloads (`delta >= 7`) allocate in checked region memory. Compiler negatives reject selected `Some(metadata)` and `Tuple2(metadata, metadata)` with unrooted heap metadata. Validation passed with compiler `575/575`, clean runtime `252/252`, and `sandbox3_next` compile at 2026-05-21 20:09-20:10 CEST. This is selected-local closure-body synthetic-factory effect-summary coverage, not broad library inference, mutable selected aliases, primitive boxed-key placement, hidden/type-only owner recovery, topology inference, or an elapsed/profile claim. |
| Closure-body optional factory heap fallback | Direct and selected `Some`/`Tuple2` closure-body factory proofs did not explicitly cover `Option.apply` null-preserving semantics or optional `Some`/`None` result branches inside a captured-owner closure body. | Compiler/runtime tests now prove captured-owner closure bodies can return `Option(new T(...))` and `if include then Some(new T(...)) else None`. Runtime stats prove the closure wrapper plus `Option.apply` non-null `Some` branch and payload allocate in checked region memory (`delta >= 3`), while the optional branch proof observes the closure wrapper plus `Some`/payload and no region allocation for `None` (`delta >= 3`). Compiler negatives reject `Option(metadata)` and `if include then Some(metadata) else None` with unrooted heap metadata. Validation passed with compiler `579/579`, clean runtime `254/254`, and `sandbox3_next` compile at 2026-05-21 20:15-20:17 CEST. This is optional closure-body synthetic-factory coverage, not broad library inference, primitive boxed-key placement, hidden/type-only owner recovery, topology inference, or an elapsed/profile claim. |
| Closure-body factory callee-summary heap fallback | Checked-region callees could already return direct `Some(new T(...))` through a captured-owner closure body, but direct `Option.apply`, direct `Tuple2`, and selected immutable local factory candidates needed explicit method/effect-summary proof. Selected method-local factory returns also needed the same source-span bridge used by closure-body selected locals; otherwise GenNIR could miss the real factory allocation sites. | `RiftRegionInference` now uses the source-span-preserving local construct owner helper for method-local returned selected factory candidates. Compiler/runtime tests prove captured-owner closure bodies calling explicit checked-region callees returning `Option(new T(...))`, `Tuple2(new A(...), new B(...))`, selected `Option.apply`, and selected `Tuple2` allocate the closure wrapper plus callee-created wrappers/payloads in checked region memory (`delta >= 3`, `delta >= 4`, `delta >= 5`, and `delta >= 7`). Compiler negatives reject direct and selected `Option(metadata)` and `Tuple2(metadata, metadata)` with unrooted heap metadata. Validation passed with compiler `587/587`, clean runtime `258/258`, and `sandbox3_next` compile at 2026-05-21 20:26-20:28 CEST. This is bounded explicit-owner method/effect-summary coverage, not broad library inference, primitive boxed-key placement, hidden/type-only owner recovery, topology inference, or an elapsed/profile claim. |
| Closure-body wrapper-returned inline closure heap fallback | Method-returned `Some(closure)` and direct closure-body returned closures were covered, but a captured-owner closure body returning a library wrapper that itself contains an inline closure still needed explicit proof. This is a small escaping-wrapper/effect-summary shape: the wrapper and inner closure must both stay in the checked region, and the inner body allocation must use the same captured owner. | Compiler/runtime tests now prove `Option((value: Int) => new T(...))` returned from a captured-owner closure body allocates the outer closure wrapper, non-null `Option.apply` `Some` wrapper, inner closure wrapper, and inner body object in checked region memory (`delta >= 4`). The paired compiler negative rejects unrooted heap metadata captured by the nested closure body. Validation passed with compiler `589/589`, clean runtime `259/259`, and `sandbox3_next` compile at 2026-05-21 20:53-20:55 CEST. Exact `Some(inlineClosure)` and selected-wrapper counterparts are recorded below; no elapsed/profile claim is made. |
| Closure-body wrapper-returned exact `Some(inlineClosure)` heap fallback | The `Option((value: Int) => ...)` inline-wrapper proof covered the null-preserving factory path, but exact `Some((value: Int) => ...)` is a distinct library factory spelling that ordinary Scala code may use directly. | Compiler/runtime tests now prove exact `Some((value: Int) => new T(...))` returned from a captured-owner closure body allocates the outer closure wrapper, exact `Some` wrapper, inner closure wrapper, and nested body object in checked region memory (`delta >= 4`). The paired compiler negative rejects unrooted heap metadata captured by the nested closure body. Validation passed with compiler `595/595`, runtime `262/262`, and `sandbox3_next` compile at 2026-05-21 21:46-21:50 CEST. This is bounded direct-wrapper closure/effect-summary coverage, not broad escaping-closure inference or an elapsed/profile claim. |
| Closure-body wrapper-returned selected closure heap fallback | Direct wrapper-returned inline closures were covered, and method-returned `Some(selectedClosure)` was covered, but a captured-owner closure body returning `Option(selected)` still lost the selected alias before the wrapper constructor safety check. The selected local looked like an erased heap `Function1` even though each candidate closure body explicitly captured the checked owner. | `RiftRegionInference` now recovers immutable selected local closure aliases nested in region-owned wrapper construction, and `GenNIR` prepares local closure values plus selected aliases under a proven closure value/body owner before wrapper constructor checks. Compiler/runtime tests prove `Option(selected)` returned from a captured-owner closure body allocates the outer closure wrapper, non-null `Option.apply` `Some` wrapper, both selected closure candidates, and selected body object in checked region memory (`delta >= 5`). The paired compiler negative rejects unrooted heap metadata captured by either selected closure body. Validation passed with compiler `591/591`, clean runtime `260/260`, and `sandbox3_next` compile at 2026-05-21 21:28-21:31 CEST. This is bounded selected-wrapper closure/effect-summary coverage, not broad escaping-closure inference or an elapsed/profile claim. |
| Closure-body wrapper-returned exact `Some(selected)` heap fallback | The `Option(selected)` selected-wrapper proof covered the null-preserving factory path, but the exact `Some(selected)` spelling is a distinct library factory path that appears in ordinary Scala code and should not rely on an unstated equivalence. | Compiler/runtime tests now prove exact `Some(selected)` returned from a captured-owner closure body allocates the outer closure wrapper, exact `Some` wrapper, both selected closure candidates, and selected body object in checked region memory (`delta >= 5`). The paired compiler negative rejects unrooted heap metadata captured by either selected closure body. Validation passed with compiler `593/593`, runtime `261/261`, and `sandbox3_next` compile at 2026-05-21 21:37-21:38 CEST. This is bounded selected-wrapper closure/effect-summary coverage, not broad escaping-closure inference or an elapsed/profile claim. |
| SafeZone root tracking in unsafe lower-bound mode | `safezone-rootless-32k` disables root add/remove. | Useful lower bound only; not a safety result. |

## Safe But Not Yet Removed

These are not code changes yet. They require a debug/headline policy and
matching compiler probes so the optimized path is not relying on undefined
behavior.

| Candidate overhead | Why it looks removable | Required proof/test before removal |
|---|---|---|
| `bucket.child.checkOpen()` in generic checked append/fold/join hot paths | Checked bucket owner tokens should prevent appending to a closed child bucket. | Removed only in the new page/token operator-owned path. Generic low-level APIs and fold/join/rank paths still need their own stale-token probes before removal. The open-region audit also found that page-token open handles are not yet statically linear after close, so public defensive checks stay. |
| `current.isOpen` check in generic current-bucket fast paths | A closed current bucket should be impossible after structured close updates arena state. | Removed only in selected page/token operator-owned paths. Other windows still need stale-current probes, and open handles must become statically linear before more public checks can be removed. |
| SafeZone roots for root-free checked rows | If region objects contain only region references or statically safe immutable/rooted heap references, the GC should not need region pages as roots. | Root-free eligibility analysis plus negative tests for unrooted region-to-heap, heap-retains-region, and outer-retains-inner. |
| HeapRoot list maintenance for root-free regions | Root-free rows should not retain dynamic heap handles. | Separate root-free backend mode that rejects `HeapRoot` use or proves all handles are static/immutable. |
| Remaining public `isOpen` checks | Low-level public region handles can still be used outside operator-owned helper paths. | Keep defensive checks until a public active/closed typestate API exists. The current compiler restriction only justifies internal/generated open-handle fast paths. |
| Checked region-body callback closure for `resetOpenHandle` | Wikimedia post-fix profiles still show a checked session-loop callback frame after boxed-ref cleanup. | Internal `resetOpenHandleInline` probes now compile and runtime-prove simple/non-inline-wrapper open-handle inferred allocation, including region-owned arrays and region-local element stores, when the final raw reset is delegated to a non-inline helper. `checked-rift-inferred` session and join modes now avoid the enclosing `inline def` wrapper through a sandbox-only bridge. The first split-only 1M Wikimedia L2 row improved inferred checked Rift to `1867.761 ms`, versus explicit checked Rift `1955.329 ms` and heap `2044.334 ms`; the follow-up state-local 1M row is inferred checked Rift `1927.010 ms`, explicit checked Rift `2015.182 ms`, and heap `2082.274 ms`. The archive-backed join control now has inferred checked `8350.524 ms` versus explicit checked `10102.728 ms`. These runs match checksum/output and keep low RSS. General compiler/inference ownership preservation is still needed for enclosing inline wrappers beyond these split framework paths. |
| General checked callback ownership beyond fixed benchmark helpers | Wikimedia session/join, StreamFlex, Theodolite, and Broom now have source-shape fixes for the profiled helper bodies, but the compiler still cannot generally preserve checked owner tracking through arbitrary enclosing inline wrappers. | Keep source-shape cleanups as benchmark/framework controls. A general optimization still needs compiler ownership/effect summaries that preserve owner captures without widening to `{any}`, plus negative tests for closure escape, widened `AnyRef`, generic hiding, stale token, and use-after-close. |

## Costs That Are Not Memory-Safety Overhead

These should not be removed by safety claims alone:

| Cost | Reason |
|---|---|
| Hash table probes in rank/fold/table operators | They are container algorithm work. Optimize layout/probing only if the operator shape is required by a winning benchmark. |
| Heap maintenance in rank/TableRank | This is ranking semantics, not region safety. |
| Object field access, token parsing, string/decimal formatting | CPU/application work. Regions only help if allocation/reclaim is material. |
| Object construction and remaining allocation lowering | Regions remove tracing/reclaim, but every object still needs header/RTTI setup, constructor work, and field stores. The page-token `allocOpen` path removes one checked branch, but it does not remove object construction or append/linking work. |
| Bulk zeroing dirty slabs on attach | Safe for zero-initialization, but not accepted as an optimization. | A 2026-05-12 focused 5M object-allocation trial was slightly worse (`70.044 ms`) than the committed object-fast baseline (`69.912 ms`) and moved time into slow slab attach (`3.277 ms`). Keep per-object zeroing for dirty reused slabs until a better zeroing policy is measured. |
| SafeZone close/reclaim bookkeeping | Required by SafeZone lifecycle unless a new reset-capable backend is designed. |

## Current Bottlenecks

| Area | Evidence | Next target |
|---|---|---|
| Checked append/window application overhead | The page-token operator clears the generated Common Crawl-shaped q1/q2 gate after open allocation: q1 checked scoped `3707.214 ms` vs heap `5577.965 ms`; q2 checked scoped `3902.795 ms` vs heap `5183.074 ms`. | Promote this shape cautiously and keep searching real WET/WAT/NDJSON/DSPBench-style inputs; generated rows prove the memory regime, not real-input prevalence. |
| Allocation lowering / object construction | `ObjectAllocationLoweringMatrix` removes stream-window/query traversal from the comparison; page-token `allocOpen`, region-open cached stats, the current-slab zeroed cache, and the stats-disabled no-stats object helper remove several runtime checks/loads from operator-owned/final-clean paths. | The no-stats object helper is the current accepted focused allocation-body win (`68.998 -> 64.485 ms` at 5M final-clean). Remaining cost is object construction, mandatory zero/init where slabs are dirty, append/linking, and query/capsule traversal. Further wins need layout/object-construction work or safe zero-elision proof, not more lifecycle bookkeeping. |
| Real streaming shared mutator floor | The 2026-05-19 mutator-parity sweep, temporary Wikimedia decompressed control, and 2026-05-20 Wikimedia/Theodolite parser follow-ups show that several real rows are dominated by byte-line parsing, hashing, decompression/input replay, numeric parsing, and benchmark source parsing rather than region close/open. | Treat shared parser/hash cleanups as fairness work for both heap and checked rows. The Wikimedia fused-parser cleanup and Theodolite byte-cursor parser cleanup are accepted on that basis only. Do not claim them as Rift-specific memory-management wins. Rift-specific follow-up should target StreamFlex allocation/init/capsule cost and checked session-loop/input shape where profiles show absolute overhead. |
| Generic checked buffer overhead | `CheckedRegionBufferMatrix` now has exact-array and fixed-capacity `ObjectBuffer` controls. At 10 x 100k records, `rift-checked-array` is `18.574 ms`, fixed `ObjectBuffer` is `25.788 ms`, and growable `RegionBuffer` is `29.968 ms`; pre-sizing `RegionBuffer` lands near `ObjectBuffer` (`25.980 ms` vs `25.408 ms`) but still trails the exact array. A follow-up fixed-chunk append operator was correct but slower than linked page-token at 1M. The 2026-05-21 source-use cleanup removes explicit record allocation plumbing from the chunk-token path by using ordinary `new` under a direct `chunkAppendRegionFor` child owner; the focused 20k heap-chunk/checked-chunk smoke matched checksum and reported checked `20632` region objects. | For known-size operators, prefer exact region-owned arrays when capacity is known. For append/drain stream workloads, keep linked page-token as the current fast path; do not assume chunking is faster unless a workload needs random access or substantially larger per-bucket payloads. The source-use cleanup is inference/API coverage, not a new chunk-token speed claim. |
| Checked rank/table overhead | TableRank and long-key rank fail focused 1M gates. The selected synthetic owner-token proof removes one accidental wrapper/payload heap fallback at checked rank/table APIs, but it does not change rank/table probing, heap maintenance, or traversal work. | Keep out of application claims; profile only after append/page/token path is fixed, and treat the new selected-synthetic proof as inference coverage rather than rank/table elapsed evidence. |
| Checked fold/aggregate overhead | `StreamWindowFold` lowers RSS/GC but loses elapsed at 1M. | Do not integrate into Common Crawl/NEXMark until focused gate passes. |
| SafeZone trace overhead | `SAFEZONE_TRACE=1` creates a severe `safezone-rootless-32k` Common Crawl q1 pathology not reproduced without tracing. | Never use trace elapsed as headline; use only counters. |

## Five-Optimization Closure Status

This closes the current pass on the five requested optimization targets. Items
are marked complete only when the safe/general path is implemented and
validated; otherwise they remain explicit gates rather than speculative runtime
changes.

| Target | Status | Evidence / next rule |
|---|---|---|
| Constructor / field-store lowering | Partially complete through the no-zero proof: constructor semantics and field stores are preserved, while redundant object-body zeroing is removed only after all fields are proven assigned before first use. | Do not elide constructors or stores. Any deeper constructor lowering needs a separate NIR proof for no `this` escape, superclass initialization, and no virtual-call side effects. |
| Proof-gated no-zero expansion | Complete for current safe record families: primitive-field records and definitely initialized reference-field records allocated through `RiftOpenStreamingHandle`. | Arrays, subclassable classes, module classes, non-local construction, control-flow exits before full initialization, impure calls, and `this` escape remain excluded. A scoped/open-region virtual-method expansion was rejected because the new calls were introduced after reachability and the generated method tables lacked the target methods; any broader checked-region no-zero work needs a reachable call target, direct raw-handle lowering, or linker-aware NIR support. |
| Traversal / cursor / capsule simplification | Implemented where ownership already proves safety: page-token cursor APIs use single-call owned cursors and no-drain close where query work is complete on append; StreamFlex capsules use bounded array transfer. | No further generic removal is justified by current profiles. Query/capsule/traversal CPU is real application/operator work unless a reusable API changes the computation shape. |
| Operator/topology specialization | Implemented as reusable checked topologies rather than benchmark-local rewrites: direct epoch, retained epoch, page-token/window, and same-shape summary-on-append controls. | Summary-on-append rows are topology/operator evidence unless the natural heap/GC comparison also shows retained-object GC/RSS pressure. |
| Root-free checked scoped backend | Still gated. Rootless/root-free rows remain lower-bound controls only. | No safe root-free claim until mixed-reference rejection covers `HeapRoot`, unrooted dynamic heap refs, heap-retains-region, outer-retains-inner, closure escape, ReML-style generic hiding, and mutable metadata hazards, and a benchmark row exists. |

## Latest Measurement

Detailed source: `evidence/OBJECT_ALLOCATION_LOWERING_MATRIX.md`.

The retained-region-array object-allocation matrix now has validated rows at 100k,
1M, and 10M objects. The result splits allocation/object-construction cost
from stream operator cost:

| Scale | Heap | Trusted Rift HP | Checked Rift | Checked SafeZone-backed | Interpretation |
|---|---:|---:|---:|---:|---|
| 100k | `1.691 ms`, GC median `0.000 ms` | `1.932 ms` | `1.576 ms` | `1.395 ms` | Checked region-array rows win once generic `RegionBuffer` overhead is removed. |
| 1M | `20.429 ms`, GC median `5.745 ms` | `19.392 ms` | `16.100 ms` | `14.347 ms` | Checked region-array rows beat heap/trusted and lower RSS. |
| 10M | `271.121 ms`, GC median `105.807 ms`, RSS `971 MB` | `199.627 ms`, RSS `404 MB` | `165.774 ms`, RSS `404 MB` | `143.319 ms`, RSS `404 MB` | Region allocation wins when heap becomes GC/RSS-bound; checked allocation is not the bottleneck in this clean shape. |

This changes the next engineering target: the region allocator itself is not
the dominant cost at 10M (`~11-12 ms` measured Rift allocator-op time), and
checked allocation is not slower in the clean retained-array shape. The earlier
checked gap came mostly from generic `RegionBuffer` retention overhead, so
checked buffers/operators need the next focused optimization pass.

Detailed source: `evidence/CHECKED_PAGE_TOKEN_COST_MATRIX.md`.

The new page-token cost-decomposition matrix splits the remaining page-token
work into `append-only`, `append-drain`, and `append-aggregate` same-shape
rows. Baseline 1M rows before the no-drain API were:

| Workload | Heap same-shape | Checked page-token | Checked scoped page-token |
|---|---:|---:|---:|
| `append-only` | `76.696 ms`, GC `11.240` | `76.831` | `74.787` |
| `append-drain` | `83.176 ms`, GC `11.147` | `84.563` | `82.569` |
| `append-aggregate` | `86.338 ms`, GC `11.523` | `87.106` | `83.484` |

After adding no-drain close and tightening the same-bucket fast path to skip
expiry checks only when the current bucket is the only live bucket, the same
1M rows were:

| Workload | Heap same-shape | Checked page-token | Checked scoped page-token |
|---|---:|---:|---:|
| `append-only` | `78.130 ms`, GC `11.508` | `77.185` | `74.743` |
| `append-drain` | `83.447 ms`, GC `11.351` | `84.698` | `81.628` |
| `append-aggregate` | `85.500 ms`, GC `11.465` | `84.928` | `82.623` |

Conclusion: no-drain close is safe, but close/traversal/open bookkeeping is
already small for this focused matrix. The remaining performance work should
focus on allocation lowering, object construction, query CPU, and application
paths that accidentally call bucket/region lookup more often than once per
bucket.

Follow-up diagnostic source:
`docs/CPU_PROFILE_REPORT.md`, `COMMON_CRAWL_WET_DIAG=1`, and
`DSPBENCH_DIAG=1`.

The 2026-05-07 diagnostic rerun clarified one misleading cost bucket:
`pageTokenAppendRegionFor` performs expired-bucket close synchronously, so raw
`bucket_switch_ms` includes most close traversal. After subtracting expired
close, estimated bucket open/switch is small:

| Row | Estimated bucket open | Close cursor | Allocation+append | Interpretation |
|---|---:|---:|---:|---|
| Common Crawl-shaped q1 checked Rift, 1M | `2.672 ms` | `757.595 ms` | `3444.176 ms` | Open/switch is not the bottleneck; allocation+append and per-record close traversal dominate. |
| Common Crawl-shaped q1 checked scoped, 1M | `9.829 ms` | `760.700 ms` | `3177.917 ms` | SafeZone-backed checked mostly wins by cheaper append/allocation; traversal is similar. |
| DSPBench Fraud q2 trusted Streaming, 1M | `0.188 ms` | `56.087 ms` | `131.493 ms` | Real-input row has negligible open cost and faster region append than heap. |
| DSPBench Fraud q2 checked scoped, 1M | `0.990 ms` | `63.456 ms` | `144.353 ms` | The checked tax is close traversal plus common query/replay CPU, not bucket open. |

Conclusion: the next useful optimization is not another bucket-open patch.
Either avoid per-record close traversal for workloads whose query work can be
completed on append, or reduce cursor/node traversal overhead itself. For real
Fraud q2, allocator work is already below heap in the diagnostic row.

Detailed source: `evidence/CHECKED_PAGE_TOKEN_APPEND_MATRIX.md`.

`StreamPageTokenAppendWindow` adds the operator-owned checked path requested by
the overhead-removal contract. It passed:

- `sandbox3_next` compile;
- checked compiler suite: `98/98`;
- checked runtime suite: `43/43`;
- 20k smoke, 100k 3-run, and 1M 3-run focused gates;
- generated Common Crawl-shaped q1/q2 100k and 1M application gates.

Focused 1M rows:

| Mode | Median ms | GC ms | RSS bytes |
|---|---:|---:|---:|
| `heap-immix` | 35.652 | 10.548 | 75087872 |
| `rift-checked-rift` | 30.819 | 0.000 | 47579136 |
| `rift-checked-page-token` | 27.141 | 0.000 | 47579136 |
| `rift-checked-safezone-improved-32k` | 29.276 | 0.000 | 47448064 |
| `rift-checked-safezone-page-token` | 26.191 | 0.000 | 47480832 |

Generated Common Crawl-shaped 1M q1/q2 rows:

| Query | Current checked | Page-token checked | SafeZone-backed page-token |
|---|---:|---:|---:|
| q1-tokenize | `4855.133 ms` | `3956.366 ms` | `3728.286 ms` |
| q2-domain-window | `4820.611 ms` | `4039.855 ms` | `3816.247 ms` |

All rows matched checksum/output count. This is generated stressor evidence,
not real Common Crawl proof.

Fixed-chunk append follow-up:

| Mode | Median ms | GC ms | RSS bytes | Interpretation |
|---|---:|---:|---:|---|
| `heap-immix` | 37.748 | 11.359 | 75071488 | Baseline linked-list shape. |
| `heap-immix-chunk` | 45.363 | 11.952 | 75104256 | Chunk traversal is slower even on heap. |
| `rift-checked-page-token` | 28.452 | 0.000 | 47579136 | Current best checked Rift append path. |
| `rift-checked-chunk-token` | 34.273 | 0.000 | 47579136 | Correct but slower due to extra chunk allocation/control overhead. |
| `rift-checked-safezone-page-token` | 27.214 | 0.000 | 47464448 | Current best checked SafeZone-backed append path. |
| `rift-checked-safezone-chunk-token` | 33.108 | 0.000 | 47579136 | Correct but slower than SafeZone-backed page-token. |

Conclusion: the chunk path is useful negative evidence. It shows that
"array/chunk" is not automatically faster than linked page-token. The
operator-owned lifetime idea is still right, but the next patch should target
the existing page-token path or exact known-size arrays rather than moving
sequential append/drain workloads to chunks.

## Next Code Patch Criteria

The next overhead-removal patch should not broaden no-check paths blindly:

1. Keep public low-level APIs defensive.
2. Add equivalent stale-token probes before applying the same owned-path idea
   to fold/join/rank operators.
3. For rootless checked SafeZone rows, add root-free eligibility checks before
   claiming safety.
4. Use focused cost matrices and profiles to distinguish object construction,
   append/linking, cursor traversal, and query CPU before changing more
   application benchmarks.
5. Prioritize real WET/WAT/NDJSON rows that stress heap GC before optimizing
   more application-specific code.
